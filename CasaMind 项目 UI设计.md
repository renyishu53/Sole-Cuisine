# SoloChef 项目 UI 设计规范

> 更新日期：2026-08-07  
> 本文档替代旧版 CasaMind 家庭事务 UI 设计。新界面围绕独居自炊用户的营养目标、三餐计划、购物清单和执行反馈设计，避免家庭协作、日历、家务看板等旧业务干扰。

## 1. 设计定位

SoloChef 是一个高频使用的个人营养与采买规划工具，界面应像“专业但不压迫的营养操作台”，而不是营销落地页或泛生活管家。

设计关键词：

```text
清晰
克制
可扫描
目标感
可信任
可解释 AI
```

体验目标：

- 用户 30 秒内看懂今天吃什么、营养是否达标、还要买什么。
- AI 生成结果必须可检查、可替换、可保存，而不是一段不可编辑文本。
- 营养、预算、偏好反馈形成闭环，让用户感到“系统越来越懂我”。
- 技术展示自然融入 Agent 轨迹和知识来源，不喧宾夺主。

## 2. 信息架构

### 2.1 主导航

| 导航 | 图标建议 | 作用 |
|---|---|---|
| 今日营养 | House / Activity | 今日目标、三餐、购物和反馈摘要 |
| AI 备餐规划 | Sparkles | 自然语言生成或调整计划 |
| 三餐计划 | Utensils | 查看每日/每周餐单，替换餐品或食材 |
| 购物清单 | ShoppingCart | 按类别查看、勾选、核销实际价格 |
| 反馈复盘 | BarChart3 | 口味、预算、营养达标率复盘 |
| 知识库 | BookOpen | 菜谱、食材、营养知识来源管理 |
| Agent 轨迹 | Network | LangGraph 节点、检索来源、校验结果 |
| 设置 | Settings | 个人画像、目标、偏好、隐私 |

移动端底部 Tab 只保留 5 个高频入口：

```text
今日营养 / 规划 / 三餐 / 购物 / 反馈
```

### 2.2 旧入口处理

从 UI 中移除：

- 家庭成员
- 家庭空间切换
- 家庭日程
- 家务任务
- 邀请链接
- 库存中心
- 通知中心

若代码暂时仍保留这些页面，不应作为主导航展示，可进入“迁移待处理”或开发调试入口。

## 3. 全局布局

桌面端：

```text
左侧导航 240px
顶部栏 64px
主内容区 max-width 1280px
右侧可选抽屉：替换食材、营养详情、Agent 详情
```

移动端：

```text
顶部标题栏
内容单列
底部 Tab
详情使用全屏抽屉
```

全局规则：

- 页面区块使用全宽背景或无框布局，不做大面积嵌套卡片。
- 卡片只用于独立信息单元，例如餐品、清单项、指标摘要。
- 卡片圆角不超过 8px。
- 所有工具按钮优先使用 lucide 图标，并提供 tooltip。
- 不使用装饰性渐变球、过度阴影或大面积单一色系。

## 4. 视觉系统

### 4.1 色彩

| Token | 颜色 | 用途 |
|---|---|---|
| Background | `#F7F8F5` | 页面背景 |
| Surface | `#FFFFFF` | 卡片、表单、面板 |
| Surface Muted | `#F1F4EF` | 次级区域 |
| Text Primary | `#1F2933` | 主文本 |
| Text Secondary | `#64748B` | 辅助文本 |
| Border | `#E2E8F0` | 分隔线 |
| Primary | `#2F7D68` | 主按钮、选中状态 |
| Primary Hover | `#256552` | 主按钮 hover |
| Protein | `#3B82F6` | 蛋白质 |
| Carb | `#D97706` | 碳水 |
| Fat | `#7C3AED` | 脂肪 |
| Success | `#16A34A` | 达标 |
| Warning | `#F59E0B` | 接近超限 |
| Danger | `#DC2626` | 超预算、忌口冲突 |

使用原则：

- 主色只服务关键操作、选中状态和进度强调。
- 宏量营养用固定语义色，不能随页面变化。
- 不用大面积渐变背景。
- 错误色只用于真正需要用户处理的风险。

### 4.2 字体

```text
中文：system-ui / PingFang SC / Microsoft YaHei
英文与数字：Inter / system-ui
```

字号：

| 场景 | 字号 |
|---|---|
| 页面标题 | 24px / 32px |
| 区块标题 | 18px / 24px |
| 卡片标题 | 15px / 22px |
| 正文 | 14px / 22px |
| 辅助文本 | 12px / 18px |
| 数据数字 | 28px / 34px |

规则：

- 后台型页面不使用 hero 级大标题。
- 字间距保持默认，不使用负字距。
- 按钮、标签、卡片内文本必须完整显示，移动端可换行但不能溢出。

## 5. 关键页面设计

### 5.1 今日营养

目标：让用户一眼知道今日是否“吃得对、买得齐、花得稳”。

模块：

- 今日目标摘要：目标热量、蛋白质、碳水、脂肪。
- 达标进度：环形或横向进度条，展示已计划/已记录摄入。
- 今日三餐：早餐、午餐、晚餐卡片，展示菜名、热量、蛋白、标签。
- 快速操作：重新规划、替换晚餐、生成购物清单、记录反馈。
- 购物状态：待购买项数、预计花费、实际花费。
- AI 提醒：只展示可执行建议，例如“蛋白差 28g，晚餐可增加鸡蛋 1 个”。

布局建议：

```text
顶部：营养目标四指标
中部左：今日三餐
中部右：购物状态 + AI 建议
底部：反馈入口 + 最近偏好变化
```

### 5.2 AI 备餐规划

目标：把自然语言需求变成可编辑计划。

模块：

- 左侧输入区：自然语言 prompt、目标模式、天数、预算、禁忌临时补充。
- 右侧预览区：三餐计划、宏量营养校验、购物预估。
- 底部操作：保存计划、重新生成、只替换不达标餐。
- 生成过程：展示 Intent、检索、Meal、Shopping、Verifier 的简要状态。

设计要点：

- 不把 AI 输出当长文本，而是结构化为餐品、营养、清单。
- 校验失败要给出明确原因，例如“午餐碳水偏高 42g”。
- 用户保存前可逐项替换。

### 5.3 三餐计划

目标：支持每日/每周查看和细粒度替换。

视图：

- 顶部日期分段控制：今天、明天、本周。
- 每天三餐固定格式。
- 每餐卡片展示热量、P/C/F、时间成本、预算估算、标签。
- 侧边抽屉展示菜谱步骤、食材克重、替代食材。

交互：

- 替换整餐。
- 替换单个食材。
- 标记喜欢/不喜欢。
- 标记已吃。

### 5.4 购物清单

目标：把食谱落实为可执行采购。

模块：

- 预算条：预计金额、实际金额、预算上限。
- 分类清单：蔬菜、肉蛋奶、主食、豆制品、水果、调味品。
- 清单项：名称、用量、预计价格、来源餐品、勾选状态、实际价格。
- 替代建议：超预算或买不到时推荐替代。

交互：

- 勾选已购买。
- 批量隐藏已购买。
- 输入实际价格。
- 查看该食材来自哪些餐。
- 一键同步反馈。

### 5.5 反馈复盘

目标：让用户看到计划有没有被执行，以及系统如何变聪明。

模块：

- 本周营养达标率：热量、蛋白、碳水、脂肪。
- 预算偏差：预计 vs 实际。
- 口味变化：喜欢的菜、拒绝的食材、常替换原因。
- 下次规划建议：减少重复、补足蛋白、收紧预算等。

可视化：

- 宏量营养使用堆叠条或小型趋势线。
- 预算使用柱状对比。
- 偏好使用标签云或列表，不使用复杂关系图作为主视图。

### 5.6 知识库

目标：管理 RAG 来源，但保持产品感。

模块：

- 菜谱文档列表。
- 食材营养来源。
- 向量索引状态。
- 图谱实体统计。
- 上传、重建索引、查看解析结果。

设计要点：

- 面向开发/高级用户，信息密度可略高。
- 明确区分 Chroma 文档和 Neo4j 图谱。

### 5.7 Agent 轨迹

目标：展示 AI 工程能力和可解释性。

模块：

- LangGraph 节点时间线。
- 每个 Agent 的输入摘要、输出摘要、耗时、状态。
- Graph RAG 命中关系。
- Chroma 检索片段。
- Verifier 校验结果。
- 错误节点和重试入口。

状态色：

- 运行中：Primary
- 完成：Success
- 警告：Warning
- 失败：Danger

## 6. 组件规范

### 6.1 指标卡

用于热量、蛋白质、预算、达标率。

规范：

```text
height: 112px
border-radius: 8px
padding: 16px
border: 1px solid #E2E8F0
```

必须包含：

- 指标名称
- 当前值
- 目标值或变化
- 状态色

### 6.2 餐品卡

必须包含：

- 餐次
- 菜名
- 热量
- 蛋白/碳水/脂肪
- 主要标签
- 操作按钮：替换、详情、反馈

交互：

- hover 展示轻量边框强调。
- 替换按钮使用 RefreshCw 图标。
- 详情使用 PanelRightOpen 或 ChevronRight。

### 6.3 宏量营养条

固定颜色：

```text
蛋白质 #3B82F6
碳水   #D97706
脂肪   #7C3AED
```

展示规则：

- 当前值 / 目标值。
- 超目标时使用 Warning 或 Danger 标注。
- 不用纯饼图表达宏量营养，避免比较困难。

### 6.4 表单

用于个人画像和目标设置。

规则：

- label 在输入框上方。
- 数值字段带单位，例如 cm、kg、kcal、g。
- 性别、目标类型、活动量使用分段控制或 Select。
- 忌口和偏好使用标签输入。
- 错误提示紧贴字段下方。

### 6.5 按钮

| 类型 | 用途 |
|---|---|
| Primary | 生成计划、保存计划、确认替换 |
| Secondary | 重新生成、编辑目标、导入知识 |
| Icon | 替换、删除、展开、复制、刷新 |
| Danger | 删除计划、清空反馈、移除知识源 |

按钮高度：

```text
Small: 32px
Default: 40px
Large: 44px
```

## 7. 状态设计

每个核心页面必须覆盖：

| 状态 | 表现 |
|---|---|
| Loading | 骨架屏，保留稳定布局 |
| Empty | 给出下一步主操作，例如“先设置营养目标” |
| Error | 说明原因，提供重试 |
| Streaming | AI 输出逐步填充结构化区域 |
| Disabled | 明确不可操作原因 |
| Success | 轻量 toast，不打断流程 |
| Conflict | 用 Warning/Danger 标出不达标项 |

空状态不写大段教学文案，只给清晰动作。

## 8. 动效规范

动效服务于反馈和层级：

- 页面切换：150-220ms fade + translateY。
- 抽屉打开：180ms slide。
- 按钮点击：scale(0.98)。
- AI 节点执行：节点逐步点亮。
- 营养进度变化：数字和条形进度 300ms 过渡。

限制：

- 尊重 `prefers-reduced-motion`。
- 不使用持续大面积动画。
- 不做 3D 装饰。
- 不用滚动叙事。

## 9. 响应式规则

桌面端：

- 左侧导航固定。
- 主内容最多三列。
- 详情使用右侧抽屉。

平板：

- 主内容两列。
- 导航可折叠。
- 指标卡改为 2 列。

移动端：

- 单列内容。
- 底部 Tab。
- 指标卡 2 列或横向滚动。
- 替换、详情、Agent 节点使用全屏抽屉。
- 表格改为列表。

## 10. 当前前端迁移建议

当前代码中 `frontend/src/router.ts` 和 `frontend/src/components/AppShell.vue` 仍有 CasaMind 家庭事务遗留。建议迁移顺序：

1. 品牌从 CasaMind 改为 SoloChef。
2. 删除家庭空间切换和邀请相关入口。
3. 主导航改为：今日营养、AI 备餐规划、三餐计划、购物清单、反馈复盘、知识库、Agent 轨迹、设置。
4. 暂时保留旧页面文件但从导航隐藏，避免一次性删除影响路由。
5. 将 DashboardView 重构为“今日营养”。
6. 将 PlannerView 重构为结构化 AI 备餐规划。
7. 将 MealsView 和 ShoppingView 作为核心 P0 页面优先打磨。
8. BudgetView、CalendarView、TasksView、MembersView 后续删除或合并到反馈/设置。

## 11. 设计验收清单

- 页面首屏能明确表达 SoloChef，而不是家庭事务规划。
- 主导航没有家庭成员、家务、家庭日程等旧入口。
- 今日营养页能同时看到目标、三餐、购物和反馈。
- AI 结果是结构化计划，不是纯文本。
- 食材替换后有营养变化和购物清单变化。
- 购物清单支持分类、用量、价格、来源餐品。
- Agent 轨迹能解释检索来源和校验过程。
- 移动端无文本溢出、按钮挤压或卡片嵌套。
- 色彩不被单一绿色或单一橙色主导，宏量营养有稳定语义色。
