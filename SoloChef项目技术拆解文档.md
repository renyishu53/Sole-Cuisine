# SoloChef — 项目技术讲解话术

> 用途：向面试官 / 技术人员**口头**讲解整个项目
> 结构约定：**每一站都先讲「业务流程」（用户做什么、系统发生什么），再讲「技术实现」（用了什么技术、怎么做到的）**
> 全程约 8~12 分钟，可按文末裁剪指南压缩
> 配套代码：后端 `backend/`（FastAPI + LangGraph），前端 `frontend/`（Vue3 + TS + Pinia）

---

## 0. 开场白（30 秒，必须背熟）

> "我介绍一个我独立完成并真实可跑的全栈 AI 项目——**SoloChef**，一个面向**独居自炊人群**的 AI 膳食与采买规划师。它把一个人吃饭最零散的几件事聚合到一个产品里：算营养目标、出一周三餐、列购物清单、吃的时候打卡反馈、再让 AI 越用越懂你。
>
> 整个产品的核心是一个**『营养目标驱动的约束式生成 + 执行反馈学习』闭环**——它不是标签式推荐，而是先算出你每天该吃多少，再把这道约束喂给 AI 去排餐，最后把你的反馈回流成下次更贴身的计划。
>
> 下面我按一个真实用户的操作顺序来讲：每走到一个功能，我先说**业务流程**——用户看到什么、系统做了什么；再说**技术实现**——背后用了什么技术、为什么这么设计。"

**为什么这样开场**：30 秒给出"是什么、谁用、能跑、有闭环"四个关键信息，并明确预告"先业务后技术"的讲解结构。

---

## 1. 第一站：项目怎么跑起来的（部署架构）

**【业务流程】**

> "用户在浏览器打开前端页面，看到的是编译好的静态页面；页面里的一切数据请求，都打到后端 API；用户的档案、计划、反馈这些结构化数据进 MySQL，会话缓存和异步任务进 Redis，菜谱知识进向量库和图谱，AI 规划跑在后端工作流里。"

**【技术实现】**

> "背后是一套 **Docker Compose** 编排的九个容器，分三类：
>
> - **应用层**：`backend`（FastAPI 主服务，跑 8000）、`worker`（Celery 异步 worker，和 backend 共享代码镜像，专门处理非阻塞任务如知识库入库、长耗时生成）、`frontend`（Nginx 托管编译产物，跑 5173 映射 80）。
> - **存储层**：`mysql:8.0`（结构化数据，异步驱动 aiomysql）、`redis:7`（缓存 + Celery broker）、`neo4j:5`（知识图谱）、`milvus`（向量数据库，它的依赖链上还有 `etcd` 做元数据、`minio` 做对象存储）。
> - **依赖编排**：backend / worker 用 `depends_on` 的 `condition` 控制启动顺序——Redis、Milvus、Neo4j 只要 `service_started` 即可，而 **MySQL 必须 `service_healthy`**（靠 `mysqladmin ping` 健康检查）后端才启动，避免后端连不上库陷入崩溃重启循环。
>
> 两个细节值得说：一是前端**多阶段构建**，node 只负责编译，最终镜像只有 Nginx 加静态文件；二是所有敏感配置走环境变量注入（`.env` + `env_file`），本地和线上跑同一份代码。"

【可能被追问】**为什么用 Milvus 而不是 Chroma？** → "Chroma 适合单机原型，Milvus 是生产级向量库、支持分布式和更大规模语料；项目早期用过 Chroma，后来切到 Milvus 做生产底座。`API接口文档.md` 里还残留着 `chroma_*` 字段名是文档滞后的小问题，代码侧已全量切到 Milvus 命名。"

---

## 2. 第二站：注册与登录（鉴权体系）

**【业务流程】**

> "用户想建档或生成计划时，会被引导到登录页。注册填手机号，点『发送验证码』，手机收到短信验证码，填上后设密码、提交就自动登录。老用户支持账号密码登录、短信验证码登录两种方式。登录成功后**七天之内都不用再登录**。"

**【技术实现】**

> "拆四层：
>
> **第一层，路由守卫 + 接口鉴权**：前端路由 meta 标记受保护页面，未登录访问跳登录页并记住目标路径，登录后跳回去；后端每个受保护接口统一从 JWT 取 `user_id`。
>
> **第二层，短信验证码**：后端调**阿里云短信 SDK** 下发，验证码存 **Redis**，有效期五分钟，另有六十秒发送冷却——都靠 Redis 的 TTL 自动过期，零清理逻辑。密码用 **bcrypt** 加盐哈希，绝不存明文。
>
> **第三层，JWT 双令牌**：登录成功签发 Access Token 三十分钟、Refresh Token 七天。为什么拆两个？只用一个短 token 用户频繁被踢出，只用一个长 token 泄露后攻击窗口太长——双令牌取长补短。
>
> **第四层，设备与会话管理**：系统有 `token_version` 概念，改密码会吊销所有旧 token；还有设备会话表，支持『踢掉其他设备』。登出时把 refresh_token 写进 Redis 黑名单，中间件每次请求先查黑名单，token 过期黑名单自动消失。"

【可能被追问】**token 存哪？** → "localStorage，这是 SPA 的常见取舍，配合前端输出转义防 XSS；更严格的做法是 HttpOnly Cookie 加 CSRF 防护，在优化清单里。"

---

## 3. 第三站：登录后的会话维持

**【业务流程】**

> "登录之后用户随意浏览、操作，完全感觉不到『登录会过期』这件事。实际上 Access Token 半小时就过期一次，但系统会在后台悄悄换新，用户无感知；只有连续七天没操作，才会被要求重新登录。"

**【技术实现】**

> "靠 **Axios 拦截器**加一个**单飞模式（Single Flight）**。请求拦截器给每个请求统一带上 Bearer Token；响应拦截器收到 401 时，这里有个真实的并发问题——页面上多个组件同时发请求，会同时收到 401，如果各自去刷新 token 就会互相覆盖。我的解法是：第一个遇到 401 的请求加锁，由它负责刷新；其他请求发现锁已存在就挂进等待队列；刷新成功后队列里的请求全部用新 token 重放。**无论多少请求同时 401，只刷新一次**。"

【可能被追问】**这个模式叫什么？** → "Single Flight 单飞，同样的思路在 Go 的 singleflight 包、缓存击穿防护里都常见。"

---

## 4. 第四站：首页（今天吃什么）

**【业务流程】**

> "进入首页，用户先看到一句问候和当天的日期，下面是『今天的三餐』卡片——每餐有菜名、预计耗时、预估花费。再往下是『今日营养进度』（还差多少热量、蛋白够不够）和『本周购物摘要』（预算多少、已花多少）。底部还有『菜谱推荐』和右下角一个问 AI 的悬浮按钮。"

**【技术实现】**

> "首页挂载时**并发**发一组请求分别拉数据，互不阻塞、谁先回来先渲染谁：
> - `GET /profile` 拿用户档案与名字；
> - `GET /meals` 拿当前活跃周计划里的全部餐食，前端再按『今天星期几』筛出当天三顿；
> - `GET /meals/today/nutrition` 拿当日营养进度（已吃 0 时就是『今日还需摄入全量』）；
> - `GET /dashboard` 拿问候语与预算；
> - `GET /budget/analytics` 拿购物摘要；
> - `GET /recipes` 拿菜谱推荐。
>
> 日期是前端 `new Date()` 实时算的，**这部分数据全部来自后端真实接口，不是写死的**。一个我后来修掉的体验问题：首页只在挂载时拉一次、不自动刷新——打卡后要看变化需要手动刷新或切页再回来，这是 MVP 阶段的有意取舍。"

【可能被追问】**首页为什么只展示今天 3 餐而不是整周 21 餐？** → "周计划里确实有 21 餐槽位（7 天 × 3 餐），但首页定位是『今天行动』，整周放在『周计划』页看更合适，否则信息过载。这点和早期把 21 餐全堆首页的设计做过取舍。"

---

## 5. 第五站：档案采集与营养目标（核心闭环第一环）

**【业务流程】**

> "用户第一次进来会先填档案：身高体重年龄性别、活动水平、目标（增肌/减脂/维持）、忌口和口味偏好、预算、厨具和能接受的备餐时长。填完页面实时算出『你每天该吃多少』——比如『2200 大卡、蛋白质 130 克，相当于 4 块鸡胸肉』。确认后这个营养目标就成了后面 AI 排餐的硬约束。"

**【技术实现】**

> "营养目标的计算是**确定性、可解释**的，不靠 LLM 猜，核心在 `services/nutrition.py`：
>
> - **TDEE（每日总消耗）**用 **Mifflin-St Jeor 公式**算基础代谢：`BMR = 10×体重 + 6.25×身高 − 5×年龄 + 性别常数`，再乘**身体活动系数 PAL**（久坐 1.4 / 轻 1.5 / 中 1.75 / 重 2.0），得到 TDEE。
> - **目标调整**：增肌 ×1.1（盈余 10%）、减脂 ×0.85（赤字 15%）、维持 ×1.0。
> - **宏量分配**：蛋白质按『体重 × g/kg 系数区间』（参考 DRIs 2023 和 ISSN 运动营养立场，减脂还要额外加量保肌肉），脂肪和碳水按剩余热量的供能比区间拆，蛋白质供能比超过 30% 自动钳制。
> - **可解释输出**：把抽象的『130 克蛋白』换算成『4 块鸡胸肉 / 18 个鸡蛋』这类等价物，帮用户建立感知。
>
> 前端 `ProfileCollectionView` 是**自动保存**（改动即写后端），`NutritionGoalView` 用 `computed` 实时算出目标并画环形图，**不需要点『应用』才生效**——这是和早期版本（手动点应用）不同的优化点。"

---

## 6. 第六站：周计划生成（重点站 · 多智能体工作流）

**【业务流程】**

> "用户点『生成计划』，填一句需求（比如『这周想多吃快手菜、预算 300』），几秒后拿到一份**一周 21 餐**的排期，每天早中晚吃什么、每餐耗时花费、配套一份购物清单和分类预算。如果点『确认』，这份计划就变成当前活跃计划，首页和周计划页都能看到；之后还能对某一餐说『换成清淡点的』，AI 局部修改并预览差异，确认后才落库。"

**【技术实现】**

> "这是全栈技术含量最高的部分，核心是 **LangGraph 编排的 11 节点状态图工作流**（`ai/workflow.py`），节点依次是：
>
> 1. `intent` — 理解用户自然语言需求；
> 2. `graph_retriever` 与 `vector_retriever` — **并行**从 Neo4j 图谱和 Milvus 向量库做 RAG 检索，把相关菜谱/营养知识拉进来；
> 3. `coordinator` — 汇总检索结果，组织成领域智能体的输入；
> 4. `meal_agent` / `shopping_agent` / `budget_agent` — **三个领域智能体**分别负责任务（下面第七站细讲）；
> 5. `domain_coordinator` — 把三个智能体结果收敛；
> 6. `planner` — 组装成周计划草稿；
> 7. `verifier` — **营养达成率校验**（实际宏量是否在目标的 90%~110% 区间）；
> 8. `final_planner` — 输出最终计划。
>
> 工作流编译时挂了 **checkpointer**（状态检查点），支持断点恢复和失败重试。
>
> 一个关键设计是**两套运行模式**：配置了真实 LLM（`llm_provider` 不为 demo 且有 key）时走大模型推理；默认 demo 模式下走 `DemoPlanGenerator` 的**确定性兜底生成**——这保证项目没有 API key 也能完整跑通演示。
>
> 计划存库时打 `is_active` 标记，并通过 `is_current_weekly_plan()` 做周归属校验（`created_at` 是否在本周 + 是否覆盖全部 21 餐槽位）。"

【可能被追问】**为什么要拆三个领域智能体而不用一个大模型？** → "三个智能体各自只解决自己领域、用各自的提示词版本，输出是强 schema 约束的结构化结果（不是自由文本），可独立测试、可确定性兜底、可审计用了哪版提示词。这是把『不可控的 LLM 调用』收敛成『可控的组件』的工程化做法。"

---

## 7. 第七站：多智能体编排的细节（重点站续）

**【业务流程】**

> "（接上一站）三个智能体各自产出：餐食智能体给出『筛选策略 + 排除的忌口食材 + 偏好的口味标签 + 每餐时长上限』；购物智能体给出『如何合并同类食材』；预算智能体给出『周预算在各分类怎么分、留多少弹性』。"

**【技术实现】**

> "三个智能体由 `ai/domain_agents.py` 的 `StructuredDomainAgentEngine` 统一驱动。每个智能体有两种运行路径：
>
> - **LLM 路径**：把提示词 + 输入拼成 JSON 请求，要求模型按固定 schema 输出，解析成 Pydantic 对象；
> - **确定性兜底路径**：即使没有 LLM，也能用规则产出合理结果（比如餐食智能体按忌口过滤、按口味标签排序；预算智能体预留 10% 弹性、按固定比例切分类）。
>
> 最关键的是**预算的硬约束兜底** `reconcile_budget()`：AI 生成的分类限额之和可能不等于周预算（这是个典型的『硬冲突』），系统**不把这个矛盾抛给用户**，而是在后端自动按比例压缩/扩展各分类限额，确保『分类之和 + 预留 == 总预算』恒成立。这样前端永远收不到一个自相矛盾的预算。
>
> 口味学习也在这里注入：餐食智能体读取 `taste_profile`（来自历史反馈聚合），把『喜欢辣、回避香菜』这类偏好直接写进筛选策略——**没有 LLM 时这套反馈也照样生效**。"

【可能被追问】**为什么不在用户输入预算时就拦截冲突？** → "因为分类限额是 AI 规划之后才有的，用户输入预算时系统还没规划，没有拦截依据。所以正确的做法不是前端拦截，而是后端生成时内部消解——这也避免了把内部约束暴露给用户造成困惑。"

---

## 8. 第八站：购物清单与食材替换

**【业务流程】**

> "周计划确认后，自动带出一份购物清单。用户看到的是**扁平的单列表**，按品类（蔬菜/肉蛋奶/主食/调味品）归类、可筛选、可改数量、可打勾采购。某样东西买不到时，点『智能替换』，系统推荐替代入口，接受后清单和原计划同步更新。"

**【技术实现】**

> "购物清单后端是 `ShoppingItem` 表，前端 `ShoppingView` 做了**扁平化**处理——不按餐分组、不嵌套二级标题，直接按品类排序展示，因为对独居用户来说『一次买齐』比『哪顿吃』更重要。
>
> 食材替换是 `substitution.py` 的服务：基于食材营养库和知识图谱找『营养相近、用户不忌口』的替换项，前端 `PlanRevisePanel` 展示差异预览。替换接受后通过 `derive_plan_with_modifications` 落库，保证『计划—清单—营养』三方联动一致。"

---

## 9. 第九站：RAG 知识库（菜谱/营养语义检索）

**【业务流程】**

> "AI 排餐和问答时并不是凭空编菜谱，而是会参考一套『菜谱与营养知识库』——比如『独居快手晚餐怎么安排』『控糖饮食原则』。用户也能自己往知识库里传文档，系统会自动入库，下次规划就能用上。"

**【技术实现】**

> "RAG 底座是 **双存储**：`Milvus`（向量语义检索）+ `Neo4j`（实体关系图谱），由 `services/knowledge.py` 的 `KnowledgeService` 统一编排。
>
> - **入库**：文档切分（`chunk_size=600, overlap=100`）后写 Milvus，同时抽实体/关系写 Neo4j；支持 `bootstrap` 幂等入库（同一文档固定 document_id，重复跑不刷屏）。
> - **检索**：`retrieve()` **并发**跑向量召回和图谱检索，可选二阶段 **reranker**（bge-reranker-v2-m3）精排；任一阵列不可用都降级（返回对应状态而非抛错），可用性优先。
> - **降级策略**：检索链路设计成对外部依赖零阻断——Milvus 挂了 RAG 返回 disabled，图谱挂了返回空，主规划链路照常走。
>
> 一个**诚实的现状**：目前 bootstrap 默认语料较薄（内置 3 篇 + 可选的 `knowledge_docs/` 目录文档，但默认不自动全量入库），所以 RAG 『能用但语料少』。这是演示规模下的已知短板，补强只需往知识库灌文档，架构不用动。"

【可能被追问】**实体抽取是 LLM 做的吗？** → "默认不是。`entity_extraction_llm_enabled` 默认 False，用本地正则抽取，避免启动期因外部 LLM 响应格式或网络问题阻塞 API；需要更丰富关系时显式开启。"

---

## 10. 第十站：执行反馈闭环（产品的立足点）

**【业务流程】**

> "用户照着计划吃：某餐吃了就『打卡』，没吃选个原因（没买到/不想吃/吃了别的），吃完还能评价『太咸了』『还想吃』。系统默默记下这些反馈——下次排餐时，AI 就会少给你安排太咸的菜、多安排你喜欢的；如果你连续三次都说某样食材不好，系统会自动把它加进你的忌口。"

**【技术实现】**

> "这是产品的核心闭环，在 `services/feedback_loop.py` 的 `FeedbackLoopService.capture()` 统一编排，分三步且**对外部依赖零阻断**：
>
> 1. **落库**：写 `plan_feedback` 偏差表，记录主观反馈与客观偏差（计划值 vs 实际值）；
> 2. **回图谱**：`(:User)-[:HAS_FEEDBACK]->(:FeedbackSignal)-[:ABOUT]->(:KnowledgeEntity)`，正负向连到 `Preference` 节点；
> 3. **回向量库**：按反馈类型维护一份滚动文档（固定 document_id），下一轮 RAG 就能召回『上次这道菜太辣』这类历史经验。
>
> 两个设计亮点：
> - **忌口自动纳入**：某食材标签连续 3 次负反馈，`apply_constraint_rules()` 自动写进 `UserProfile.constraints`——这就是『越用越懂你』的落地。
> - **口味画像**：`FeedbackRepository.taste_profile()` 把历史反馈聚合成 `TasteProfile`（喜欢的标签/回避的菜品），注入餐食智能体提示词，且**确定性回退路径也直接用它排序**，所以没有 LLM 反馈照样被学习。
> - **补偿重放**：图谱/向量库暂不可用时只打 `synced=False` 标记，恢复后可由补偿任务重放，不丢数据。"

---

## 11. 第十一站：AI 对话助手（SSE 流式）

**【业务流程】**

> "任何页面右下角有 AI 悬浮按钮。用户问『推荐个适合减脂的快手午餐』，回答不是转圈后一次性弹出，而是**像打字一样逐字出现**。支持开多个会话、查历史、删会话，下次打开对话还在。还能上传一张食物照片让 AI 识别。"

**【技术实现】**

> "这是 **SSE 流式输出**，链路三段：前端用原生 `EventSource`/`fetch` 流读后端；FastAPI 返回 `StreamingResponse`；后端在生成过程中把 token 逐帧推给前端。
>
> 工程细节：
> - **断线恢复**：`streamChat` 边读边记 `lastEventId`，网络中断时调 `replayChatEvents` 从断点续传，最多重试 3 次，用户无感知。
> - **会话持久化**：对话历史在流结束后落库（`ChatSession`/`ChatMessage`），支持多轮上下文。
> - **多模态**：`/chat/vision` 接 Qwen-VL（默认关闭，可配 key 开启），上传图片让 AI 识别食材/菜品。
> - **换菜也是它**：用户自然语言的『把周三午餐换成清淡的』会被解析成结构化修改操作，走 `plan_revise` 的『预览差异 → 确认落库』流程，复用同一套对话持久化。"

【可能被追问】**为什么用 SSE 不用 WebSocket？** → "AI 对话是『一次请求、服务端持续推』的单向流，SSE 是纯 HTTP、浏览器原生支持、断线自动重连，够用；WebSocket 的双向能力用不上，是过度设计。"

---

## 12. 第十二站：报告与画像

**【业务流程】**

> "周末用户打开『周报』，看到这周营养达标率、花了多少钱、哪几餐没吃、下周该注意什么；切到『日报』则是当天吃了啥、打卡和反馈回顾。"

**【技术实现】**

> "`GET /reports/weekly` 由 `services/weekly_report.py` 聚合：营养达成率来自 `nutrition.build_nutrition_report`（实际宏量 / 目标，按 90% 达标线判断）、执行覆盖率、花费对比、下周行动建议。日报模式复用同一组件的不同数据源。
>
> 一个**有意的减法**：早期版本里有『口味演化』（本周 vs 上周标签对比）和『口味画像』两个区块，但反馈数据积累到有意义量级前它们长期是空态，只会让产品显得像半成品——MVP 阶段我选择先把这两个展示区块拿掉，底层的口味学习闭环保留不动。"

---

## 13. 收尾话术（30 秒）

> "总结一下：业务上，这是一个**『营养目标 → AI 排餐 → 购物清单 → 打卡反馈 → 学习闭环』**的完整产品。技术上我最想讲的三个点是：
> - **LangGraph 11 节点 + 三个领域智能体的可编排 AI 架构**，每个智能体都有 LLM 和确定性兜底双路径，且预算硬冲突在后端内部消解；
> - **Milvus + Neo4j 双底座 RAG**，对外部依赖零阻断降级；
> - **执行反馈学习闭环**，把打卡/评价回流成图谱、向量库和口味画像，驱动下次更贴身的计划。
>
> 不足我也复盘过：默认语料偏薄（需补强知识库）、首页不自动刷新、周计划轮转还靠手动确认（已有 `is_current_weekly_plan` 周归属判断但首页活跃计划查询未自动用上）、部分文档滞后于代码（如 Chroma 字段名）。每条都有明确改进方案。我的介绍到这里，谢谢。"

---

## 14. 应急问答库（被打断时使用）

| 打断问题 | 一句话接住 |
| --- | --- |
| "最大难点是什么？" | "把『不可控的 LLM』收敛成『可控组件』——领域智能体强 schema 约束 + 确定性兜底 + 预算内部消解，让系统在没 key 时也能跑、有 key 时更聪明。" |
| "数据一致性怎么保证？" | "结构化数据靠 MySQL 事务 + Alembic 迁移；反馈回流对 Neo4j/向量库零阻断降级 + `synced` 标记补偿重放；图谱与向量库有一致性报告接口可观测。" |
| "周计划过期怎么办？" | "底层已有 `is_current_weekly_plan` 周归属判断，但首页活跃计划查询还只看 `is_active`，下一步是把周过滤接进活跃计划查询 + 前端过期引导生成新一周。" |
| "为什么用 Vue 不用 React？" | "都了解，Vue3 + TS + Pinia 对这个规模 SPA 开发效率高，技术选型没有执念。" |
| "安全性做了哪些？" | "bcrypt、JWT 双令牌 + 设备会话吊销、所有『我的 XX』接口从 token 取 user_id 防越权、阿里云短信验证。不足是密钥管理和限流在优化清单。" |
| "这是 demo 还是真项目？" | "真实可跑的全栈 MVP——FastAPI/MySQL/鉴权/CI 全齐；默认 demo 模式（确定性强、无需 API key 即可演示），配真实 LLM key 即变可用形态。" |
| "重做会改什么？" | "补强 RAG 语料、首页自动刷新与周轮转自动化、密钥入配置管理、补端到端测试体系——对应数据、体验、安全、工程化四个维度。" |

---

## 15. 时间裁剪指南

| 可用时长 | 保留 | 砍掉 |
| --- | --- | --- |
| 3 分钟 | 开场 → 第 2 站（登录）→ 第 6 站（周计划生成，压缩）→ 第 10 站（反馈闭环）→ 收尾一句 | 其余全部 |
| 5 分钟 | + 第 3 站（单飞）、第 7 站（多智能体细节）、第 9 站（RAG） | 购物、报告、视觉细节 |
| 10 分钟 | 第 1~12 站全文 | — |
| 汇报场景 | 第 1、6、10 站配架构图讲，其余一张功能全景图带过 | 口语细节 |

---

*配套文档：`SoloChef项目技术拆解文档.md`（本文件，讲解话术版）｜可另出《SoloChef 架构深度版》（含完整端点清单与数据模型）*
