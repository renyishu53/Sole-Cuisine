# 2026-08-14

## 两个 AI 入口定位纠正（重要设计决策）
用户指出截图诊断中"AI 入口 2→1 合并为仅 FAB"的结论错误。核实后端代码后确认两个入口职能不同、不应合并：

- **右栏「修改对话」→ `PlanReviseService`**：作用域=当前周计划，NL→结构化 `ReviseOperation`（7种）→ diff → confirm 派生新版本（**写**操作）。复用 ChatSession/ChatMessage 仅持久化历史，本质是有对话外壳的"改计划工具"。
- **FAB「问 SoloChef」→ `ConversationService`**：全局浮窗，**只读**问答，`plan=None` 不改业务数据，走 RAG(Milvus+Neo4j)+DeepSeek。
- **视觉 `VisionService`（Qwen-VL）**：独立 VLM，4 场景（食材/菜品+热量/营养标签/小票 OCR），**已实现但尚未接入 FAB 聊天流**——是待补集成项，非已完成功能。

**修正结论**：保留两个入口，重新定位——右栏=计划微调工具（写），FAB=全局只读助手+视觉（读），互补不冗余。混乱根源是右栏命名「修改对话」像聊天，应改名「调整计划/修改备餐」并露计划上下文。边界：FAB 永只读（后端已 plan=None），一旦 FAB 能改计划才真正重叠。

之前 8-13 截图诊断的"N2 双AI入口→2→1"建议作废。

## 双 AI 入口前端组件规范（文字输出，未落文件）
基于 python-patterns/frontend-dev/ui-ux-pro-max 三技能整理，用户要求纯文字不写文件。要点：
- **组件一 `PlanRevisePanel`**（右栏·计划微调工具，写）：命名改「调整计划/修改备餐」（弃用"修改对话"）；仅 PlannerView 右栏常驻、露计划上下文；后端 `POST /plans/{id}/revise` + `/confirm`，派新生新版本；边界：不跨计划/不全局问答/不视觉。
- **组件二 `AskSoloChefFab`**（全局浮窗·只读助手，读）：所有页面右下角 z=50；`ConversationService` 只读 `plan=None`；边界：禁写、禁改计划、改计划类问题只"指路"到 PlanRevisePanel；视觉 VisionService 已独立实现但未接入 FAB，UI 先灰态/标注。
- 强制：显式命名、SoC、最小惊讶、三态完备(Loading/Empty/Error/Tactile)、44px 触控、aria-label、focus ring、z-index 标尺、异步禁用、reduced-motion、禁 emoji 用 SVG。

## 两份规范合并为「SoloChef 前端组件规范（合并版）」
用户要求把「双 AI 入口 · 前端组件规范」与「备餐规划页面 · 组件布局与命名重设计（5截图诊断）」合并成一份规范页，并明确"两个 AI 对话窗口位置先不做更改"。
- 关键修订：粘贴的备餐规划方案原本"方案二=AI 入口 2→1 干掉右栏"与既定"保留双入口"结论冲突 → 已改写为"保留双入口、重命名（修改对话→调整计划）、位置暂不变"，同步修正命名对照表+变更对比的 AI 行。
- Tab 5→3（今日/本周餐食/购物）、版本→右上角「版本▾」抽屉、今日概览合并入「今日」Tab、操作按钮按频率分层（高频外露/中频溢出/低频确认）均保留。
- 本次以纯文字输出，未落文件。如需落盘 `SoloChef 前端组件规范.md` 待用户确认。

## FAB 与右栏位置冲突（已读代码定位根因，提出方案）
- 根因：`AskSoloChefFab.vue` 锚点写死 `right:26px;bottom:58px`（z-51），而 `PlannerView.vue` 右栏 `aside.plan-revise-chat`=PlanRevisePanel 铺满右侧，其 `mode-focus` 态 `fixed;right:14px;z-30` → 在 `/planner` 浮标叠住右栏底部 composer/CTA。
- 推荐方案 A（上下文感知停靠）：复用既有 `inPlanner` 计算属性，planner 页 FAB+面板切到 `left:26px`（dock-left），其余页保持右下；补 `:focus-visible` 焦点环 + 一次性 `fab-discover` 脉冲（reduced-motion 关闭）。4 处改动（模板类绑定 / dock-left CSS / 移动端切左 / focus+discover）。
- 备选 B 全局固定左下；备选 C 底部居中 Dock（需确认无底栏争位）。
- 截至 2026-08-14 仅提出方案，未落地文件。

## 反馈复盘"按周延迟"讨论（用户 UX 洞察 + 事实校正）
用户提出：反馈复盘按周统计有及时性短板。经核对代码**校正事实**：
- 反馈复盘页(`FeedbackView`)**并非周桶**，而是滚动近实时（近期反馈=最近30条无时间窗；sentiment_counts 全量累计；taste_profile 最近40条）。
- 真正"按周"的是**阶段性报告**(`/reports/weekly`)，属刻意回顾型设计。
- **真缺口**不在统计周期，而在"动作点即时闭合感"弱：MealsView 打卡/评价后仅 toast 显示 sample_size，用户不知"这句太辣具体落到了 disliked_tags 还是触发了忌口自动纳入"，行为→结果因果链路不可见 → 削弱行为关联感。后端 `FeedbackSyncResult.notes`(忌口纳入提示)已返回但未在动作现场接出。
- 候选修复(未落地)：A 动作现场结构化回执(最重要)；B 复盘页=实时仪表盘/周报=深度回顾的文案区分；C 今日Tab露"最近反馈已让下周规避X"。
- 用户对"及时反馈强化行为关联"的 UX 原则判断正确，但落点应是动作现场即时闭环，而非改复盘页统计周期。

## 业务流程极简收敛（2026-08-14 16:13，用户反馈"流程过多理不清"）
- 根因：前几轮把流程拆太细(六阶段+7决策)、方案叠多版，信息堆成负担遮住主干。
- **收敛为"一个闭环 4 阶段"**：[0 入驻设标]→[1 生成计划]→[2 执行打卡]→[3 反馈优化]→反哺。[0]一次性可改；[3]含忌口/口味画像反哺下轮。
- **六阶段+7决策(D1-D7)降级为交互策略**（怎么做），不列为独立流程步骤。
- **建立"业务↔页面↔组件"三级映射**：页面仅 4 个(个人档案/备餐规划[今日·本周餐食·购物 3Tab]/反馈复盘/周报)；每页 2-3 核心组件，职责不重叠。
- **规划组件三条铁律**：①一屏一主事 ②动作在现场闭环(打卡后当场显示"学到什么") ③复杂度按频率分层(平级按钮≤3)。
- 用户一贯要求纯文字不落文件；本次仍文字输出，未写文件。

## 业务流程主题边界澄清（2026-08-14 16:16，用户指出4阶段偏离"AI独居膳食与采买规划"）
- 用户反馈：把"入驻设标"+"反馈优化"当平级流程阶段，会把范围扯宽、偏离项目主题。判断正确。
- **修正为聚焦版 4 阶段（全直接服务"膳食与采买规划"）**：[1 定营养目标(约束输入)]→[2 AI生成计划]→[3 调整计划]→[4 执行采买]。
- **"入驻"降级**：剥掉用户管理味，只留"营养目标"作规划输入约束；个人档案页=目标配置低频入口，属[1]一部分，非独立阶段。
- **"反馈优化"降级为横切隐性回流回路**：嵌入[4]执行(打卡/评价触发)→回流[1][2]输入(口味画像/忌口)；非独立第5阶段。反馈复盘页/周报=回路可视化层(建立信任)，非主流程步骤。
- 与项目定位原话"核心闭环：营养目标→AI三餐→购物清单→食材替换联动→执行反馈学习"完全对齐（反馈学习=末端隐性回流）。
- 页面性质厘清：主流程页(备餐规划) / 目标配置入口(个人档案) / 回路可视化(复盘+周报)，三者不平级当成"4个流程页"。

## 主题边界二次澄清（2026-08-14 16:20，用户指出"定营养目标"仍偏离"不知道吃什么"）
- 用户洞察：项目针对"独居自炊不知道吃什么"，若流程第一环是"定营养目标"，等于把"自我管理工具"壳套在"帮我决定吃什么"魂上，偏离主题。判断正确。
- **最终聚焦版 3 显性阶段**：[1 看规划(AI直接给今日/本周吃什么+买什么)]→[2 调整]→[3 执行采买]。进门即有答案，不定目标也能用。
- **营养目标降级为"后台引擎+可选后置调参"**：默认"健康维持"档即开即用；精细化(减脂/增肌)用目标chip跳档案页，非进门门槛。呼应 early 定位"目标是引擎不是门面"。
- 与项目定位"营养目标驱动约束式生成"不矛盾：目标在后台驱动生成，但用户感知层第一眼是"餐食建议"而非"填表"。
- 页面：首页/今日Tab=[1]入口(场景化非人群化)；备餐规划页=承载[1][2][3]；个人档案页=目标精细化可选后置入口；复盘/周报=回路可视化。
- 至此主题边界彻底钉死：解决"独居自炊不知道吃什么"，营养目标是让答案更准的隐形引擎。

## 业务流程框架升级为「两分法」（2026-08-14 16:22，用户仍说"思路乱"）
- 根因诊断：一直用"流程阶段"叙事打补丁（六阶段→4阶段→3阶段+隐形引擎），框架本身晃动，用户记不住终版。
- **改用稳定框架，弃用"第N步"叙事**：SoloChef = "持续自优化的规划引擎 + 极简使用界面"。
- **北极星**：SoloChef 替独居自炊的人想好"今天吃什么、买什么"，且越用越懂你。
- **产品两面**：
  - 用户面（规划组件唯一服务对象，3动作）：看建议 / 调一下 / 打完卡。无"入驻""反馈"阶段。
  - 系统面（全隐形）：营养引擎（默认健康维持档即开即用，精细化可选后置）+ 反馈学习（打卡评价时后台学忌口/口味，下次规避）。
- **铁判据（任何组件决策套用）**：沾用户面→做进主界面且按频率分层；沾系统面→藏后台/可选后置，绝不作进门动作；两头不沾→偏离主题砍/降级。
- 此框架终结"算不算一个阶段"的反复纠结；之前所有诊断乱象（N/M/S/V类）均归为"用户面内职责未切干净/频率未分层"，用判据直接修。

## 业务流程定稿（2026-08-14 16:25，用户给项目介绍原文作权威锚点）
- 用户原话："面向独居自炊人群，基于营养目标驱动与多智能体协作的AI膳食与采买规划系统，实现了用户画像解析、营养素分配、食谱匹配与购物清单生成的完整闭环，让'吃得对'变成可坚持的日常习惯。"
- **引擎侧业务流程定稿（4步闭环）**：[1 用户画像解析]→[2 营养素分配(Mifflin-St Jeor算TDEE+宏量)]→[3 食谱匹配(meal智能体)]→[4 购物清单生成(shopping+budget智能体)]→反馈回流→下一轮[1]。
- 多智能体落点：meal=食谱匹配；shopping+budget=购物清单生成；LangGraph 11节点StateGraph编排。
- 闭环=规划→执行→学习→再规划（呼应"可坚持的日常习惯"）。
- 与两分法一致：[1-4]是系统面(引擎)后台跑；用户面仍是看建议/调一下/打完卡三动作。
- 锚定既往担忧：[1]是引擎侧输入解析非用户填表；反馈/入驻在闭环里是回流/输入更新非独立阶段；流程严格源自介绍原文不偏离主题。
- 用户最终给定**权威业务流程定稿（6 阶段完整闭环）**：①入驻建档→②营养素分配→③食谱匹配(11节点LangGraph+双路检索+三智能体+Verifier[90%,110%])→④购物清单生成→⑤执行与反馈(确认落库/局部修订diff)→⑥学习回流(图谱子图+向量滚动窗口30+口味画像近40+/resync补偿)→回③。含关键词↔阶段映射表与三条主线(营养目标驱动/多智能体协作/执行反馈闭环)。
- 代码核对：6 阶段全部后端已实现；注册/登录密码+短信双通道、JWT access+refresh 双 token 已核实(auth.py/security.py/sms.py)；⚠️定稿文档原写"Neo4j + Chroma"有误，实际已迁 **Milvus**（仅 milvus_store.py，无 chroma_store.py），落盘前须改。
- 与"两分法"收口：6 阶段=系统面（含一次性入驻），用户面仍为看/调/打卡三动作；入驻是一次性系统步骤，不破坏"开门即有答案"体验，故此前"入驻/定目标当第一环跑题"的担忧在此框架下消解。
- 反馈回流双通道：主回③(口味画像注入meal agent prompt)，同时回①(连续3次负向→自动写入UserProfile.constraints忌口)。
- 用户一贯要求纯文字不落文件；本次仍文字输出，未写文件。待用户确认后可落盘 `SoloChef 业务流程定稿.md`（含 Milvus 修正）。

## 闭环完整性评估（2026-08-14 16:28，用户问"是否能形成完整闭环"）
- 结论：**后端已真正实现完整闭环**，非PPT。逐环核对均代码落地：[1]UserProfile/[2]nutrition.py(Mifflin-St Jeor)/[3]meal智能体/[4]shopping+budget智能体/执行capture()/学习feedback_loop.py(回Neo4j+Milvus+taste_profile,降级不阻断)。
- 再规划反哺两条通：①口味画像→注入meal agent prompt(下次规避) ②连续3次负向→自动纳入UserProfile.constraints(忌口)。营养目标[2]不被反馈自动改=设计选择非缺陷。
- **主要距离=前端体验层未落地**：后端闭环通，但用户"执行打卡/看到学到什么"界面仍是之前诊断的混乱态(N/M/S/V类)，方案有未写码。即逻辑闭环完整，用户体感断在"执行/复盘"头。
- 增强项不影响主干：VisionService未接入FAB、reranker降级、G07/G08/G11/G14/G15可选深化。
- 下一步候选：①落盘定稿md ②前端动手落地(补执行/复盘体验)。
