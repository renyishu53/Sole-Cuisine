# 2026-08-21 工作日志

## 项目智能体相关澄清（重要，纠正简历/文档口径）
- 全量搜证确认：项目**没有实现自主智能体**（无 bind_tools/ReAct/AgentExecutor/agent 运行时）。
- 文档里的「多智能体」= 代码里真实存在的「三领域结构化组件(meal/shopping/budget) + 单点 planner + 确定性 verifier，由 LangGraph 编排」；文档已自声明"非自主、无写库权限"——审查没问题是对的。
- 项目**根本不存在 "deepagent"**；所有 "deep" 字样 100% 是 LLM 供应商 DeepSeek（config.py: llm_model="deepseek-chat"）。
- 用户疑问"为什么审查说多智能体没问题" → 答案：文档用的是宽松定义(命名/设计模式标签)，代码与文档一致，不矛盾。

## 智能问答 Agent 改造方案（已产出，未落代码）
- 结论：核心流程没必要上自主 agent；但**「问 SoloChef」咨询问答最适合加 tool-calling 轻量 ReAct agent**（开放多步推理 + 只读零风险）。
- 方案要点：新增 `backend/app/ai/chat_agent.py` + `conversation_tools.py`；用 `langgraph.prebuilt.create_react_agent`；6 个只读工具(search_knowledge/get_user_profile/get_nutrition_goal/get_current_plan/compute_recipe_nutrition/analyze_budget)，全部闭包绑定 session+user_id 严格隔离；`stream_turn_agent` 用 `astream_events` 发 tool_call/tool_result/token SSE；config 加 `chat_agent_enabled`(默认False)；异常回退现有单射；测试用 fake model 不依赖真实 LLM。
- 用户尚未确认是否动手实现（默认只给方案，等确认）。

## 其他已澄清
- recipes 表为空是设计（用户私有，靠 API 写）；展示菜谱在 recipes.json(12条)。前端无自建菜谱页面（createRecipe 已定义但无 .vue 调用）。
- domain_agents_llm_enabled=False = 三领域节点走确定性规则不调 LLM；只影响领域节点，不影响 planner 单点 LLM。

## 核心生成工作流「多 agent 化」重构评估（用户主动询问，已给方案，未落代码）
- 结论：技术上可行、风险可控。把 meal/shopping/budget 三领域节点从"受控函数/单点LLM"升级为 `create_react_agent` 真 ReAct agent（bind_tools + 反思循环），新增 supervisor 做任务分解与 inter-agent 协商，planner 升级为 agent；verifier 保留确定性兜底（营养硬约束不交 LLM）。
- 工具层范围：只读检索(search_recipes_vector/search_knowledge_graph/get_user_profile/get_nutrition_goal/find_substitutions) + 计算(estimate_meal_nutrition/estimate_budget/reconcile_weekly_budget) + 校验(check_constraints)。**不暴露写库工具**（一致性安全边界）。
- 与日志既有"核心流程没必要上自主 agent"不冲突：那是成本/安全默认建议；本次是用户主动要可行性方案。落地建议：作为可选深化亮点，务必保留 verifier 兜底 + 降级。

## 工作流+问答「多 agent + 外部工具(Tavily)」扩展评估（用户进一步要求，已给方案，未落代码）
- 范围：用户要求**工作流与智能问答都重构为多 agent**，并接入外部工具（如 Tavily 联网搜索）。已 grep 确认项目当前无 tavily/serpapi/brave 任何集成，方案为新增。
- 统一工具层 = 内部只读/计算/校验工具 + 外部 `search_web`（tavily-python 封装，需 `TAVILY_API_KEY` + `tavily_enabled` 开关 + 超时降级 + 限频限结果数）。
- 问答 agent：ReAct + 内部只读 + Tavily，最自然低风险切入点（开放推理 + 零写库）。
- 生成工作流：三领域 ReAct agent + supervisor；Tavily 仅作**可选第三召回路/参考 context**，**不参与硬约束**，最终仍过 verifier 营养/忌口兜底。
- 边界：外部工具需 key+开关+降级；生成流联网内容不得突破营养硬约束；问答流引用联网结果需标注来源 url。

## 用户整合方案问答（16:39，已口头回答未落码）
- 用户把"简化11→5 + supervisor多agent + Tavily web_research"整合为三阶段方案，问：几个工作流 / 流程图 / 是否真 agent+外部工具。
- 答：项目共 **3 条执行链** = 生成图(内部静态5节点与 supervisor 版是两模式同图，靠 `workflow_supervisor_enabled` 切换) + 调整图 `PlanRevisionWorkflow`(8节点独立) + 问答 ReAct Agent(独立)。
- 真 agent / 外部工具是方案**目标态非现状**：supervisor 满足决策点+inter-agent 协商判据、web_research 绑 Tavily 为真外部调用；但默认关(`workflow_supervisor_enabled`/`tool_websearch_enabled`=False)，verifier 始终确定性兜底。

## 工作流节点简化评估（用户嫌 11 节点过多，已给方案，未落代码）
- 结论：可简化，11→4~5 节点，能力不丢、低风险（只合并薄 pass-through 节点，逻辑下沉到节点内/Service，不改表）。
- 合并点：graph_retriever+vector_retriever+coordinator → `retrieve`（内部 asyncio.gather 双路+拼 context）；meal/shopping/budget+domain_coordinator → `domain_agents`（内部 gather 三引擎+合并 bundle）；final_planner → 并入 verifier 末尾写 sources；constraint_parser → 入口注入 planning_constraints（PlanningService 直接算）。
- 关键：并行性不退化(asyncio.gather)、trace/降级保留、planner 与 verifier 不合并(职责不同)。与多 agent 重构正交，简化后各节点仍可升级 ReAct。
