# 2026-08-19 工作记录

## SoloChef 全量技术拆解（只读、未改动文件）
- 用户要求：阅读项目全部文件，输出（1）交付部署就绪度评估 （2）业务流程拆解 （3）全技术模块详细拆解（不限于工作流）。明确禁止写入/更改任何文件。
- 核对了源码以代码实证结论：
  - 主工作流 `ai/workflow.py::_build_graph` = **11 节点**（constraint_parser, graph_retriever, vector_retriever, coordinator, meal/shopping/budget_agent, domain_coordinator, planner, verifier, final_planner）。交付评估文档称“12 节点”与源码不符，以 11 为准。
  - 调整工作流 `ai/revision_workflow.py` = **8 节点**（revision_intent + 5 个 RevisionRoute 路由节点 MEAL/SHOPPING/BUDGET/CONSTRAINT/COMPOUND + affected_agents + dependency_sync）。
  - 14 张表已核实（models/identity.py）。docker-compose 实际含 9 服务（backend/worker/frontend/mysql/redis/etcd/minio/milvus-standalone/neo4j）。
- **关键发现**：git 工作树存在大量未提交改动（74 文件，+25,409/-2,846 行），分析报告 v9.3(08-11) 称 pytest 55 passed 反映的是已提交基线；交付评估(08-18)称全量 pytest 未通过（temp 目录权限/计划校验/分段规划触发真实 LLM 401）。两文档结论背离的根因即这批未提交改动。
- 已读核心模块：planning.py / domain_agents.py / plan_validation.py / nutrition.py / llm.py / knowledge.py / feedback_loop.py / substitution.py / plan_revise.py / conversation.py / runtime.py / worker.py / api.ts。
- 结论：业务闭环完整、前端可交付验收，但当前态不可直接生产交付（测试未通过+未提交改动+密钥在 .env+断点续传 InMemorySaver 生产失效）。
