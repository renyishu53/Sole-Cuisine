# 2026-08-22 工作日志

## 代码现状核查：生成计划工作流是否真·多 agent（重要更正）
- 用户要求"重新读代码判断生成计划工作流是否实现基于 LangGraph 多 agent 编排"。
- 核查发现代码在 8/22 已被改动（早于本会话）：新增 `backend/app/ai/agent_tools.py`，`workflow.py`/`domain_agents.py`/`llm.py` 均有更新。比 8/21 结论"仅方案未落地"进了一步——代码已落地。
- 关键事实（基于实际文件）：
  - 开关默认全 False（config.py:59-68）：`workflow_supervisor_enabled=False`、`tool_websearch_enabled=False`、`domain_agents_llm_enabled=False`。
  - 默认图拓扑（workflow.py:140-177）：`retrieval → [meal∥shopping∥budget] → planner → verifier`，无 supervisor，纯确定性编排，**非多 agent**。
  - 领域节点 `domain_agents.py`：仍是 `ChatOpenAI.bind(response_format=json_object)` 单点 JSON 生成，**无任何 bind_tools / 工具调用**。
  - supervisor 模式（开 `workflow_supervisor_enabled`）：`_supervisor_node`(workflow.py:475-562) 用 ChatOpenAI+JSON 解析做动态派发 + 多轮补派（verifier 失败→回 supervisor，max_rounds=2、max_total_dispatches=6）；`_web_research_node`(614+) 接 Tavily，门槛触发。这是**货真价实的 Supervisor 多 Agent 架构**（独立 supervisor agent + 独立 meal/shopping/budget 专家 agent + LLM 动态派发 + verifier 反馈循环协商 + 硬上限），属于"受控编排 / Supervisor 模式"多 agent，是标准多 agent 拓扑之一。
  - **真正的 tool-calling 真 agent 在问答服务**：`llm.py ConversationalGenerator.answer`(277-310) 用 `bind_tools` + `tool_calls` 循环消费 `agent_tools`，是真 ReAct agent，但属于问答链，不是生成工作流。
  - Tavily 已接入 `agent_tools.web_research` 并接到 workflow `web_research` 节点 + 问答 agent，但 `tool_websearch_enabled=False` 默认关，且生成流仅 supervisor 模式才触发。
- **校准结论（用户 .env 已 `WORKFLOW_SUPERVISOR_ENABLED=true` + `DOMAIN_AGENTS_LLM_ENABLED=true` + `TOOL_WEBSEARCH_ENABLED=true`）**：代码默认值是 False（新环境安全默认），但**用户实际运行环境已是 Supervisor 多 Agent 路径 + Tavily 启用**，非默认静态路径。所以"项目默认不是多 agent"只对代码默认值成立，对用户当前实例不成立——此前我聚焦 config.py 默认值而忽略 .env 覆盖，表述有误。
- **对外口径（校准后，与另一对话框一致）**：生成计划模块 = LangGraph 编排的 **Supervisor 多 Agent 架构**（supervisor 动态派发餐食/采购/预算专家 + verifier 确定性校验决定是否进入下一轮协商）；当前领域专家是**结构化 LLM Agent，不具备自主工具调用能力**（`bind(response_format=json_object)` 而非 `bind_tools`）；问答模块已实现**基于工具调用的真 ReAct Agent**（`llm.py` bind_tools 循环）；联网搜索已接入，由配置 + 实时门槛控制。即"是 Supervisor 多 Agent（受控），非开放式 ReAct/Agent Swarm；专家工具调用仍无，真 tool-calling 仅问答链"。
- 与 8/21 的修正：当时我说"多 agent 仅方案未实现"，现代码已落地（supervisor + agent_tools + 问答 bind_tools + Tavily 接线），但默认关且领域节点仍非 tool-calling。
