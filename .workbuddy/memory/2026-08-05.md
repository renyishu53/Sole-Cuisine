# 2026-08-05 工作总结

## 1. 阿里云短信服务修复
- **问题**：后端 `sms.py` 误用 Dysmsapi（短信服务，模板 CODE 形如 `SMS_xxx`），但实际配置的是号码认证服务 Dypnsapi 模板（100001-100005），导致"该账号下找不到对应模板"
- **修复**：将 SDK 从 `alibabacloud_dysmsapi20170525` 切换为 `alibabacloud_dypnsapi20170525` v2.0.0，endpoint 改为 `dypnsapi.aliyuncs.com`，请求对象改为 `SendSmsVerifyCodeRequest`
- **真实验证**：2026-08-05 15:41 向 17267259522 发送测试验证码，返回 `code: OK / success: True`，短信发送成功
- **依赖**：pip 安装成功；uv lock 因 Windows Recycle Bin 不可用导致 safe-delete 失败（已知 uv bug），不影响运行环境

## 2. 前端技术栈信息脱敏
- 全部 11 个文件中的技术术语改为用户友好文案：
  - `Graph RAG` → `懂你的家`/`知识检索`; `多智能体协同` → `智能协同规划`
  - `Neo4j` → `关系图谱`; `Chroma` → `知识库`; `LangGraph` → `规划引擎`
  - `PostgreSQL` → `自动保存`; `LLM` → `AI 模型`; `JWT` → `登录状态已加密保护`
  - `Agent Trace` → `规划轨迹`; `Access Token 30 分钟` → `登录状态已加密保护`
- 涉及文件：router.ts, AppShell.vue, AuthView.vue, AgentView.vue, KnowledgeView.vue, MembersView.vue, CalendarView.vue, ChatView.vue, PlannerView.vue, ShoppingView.vue

## 3. 登录页排版美化
- 卡片宽度：`390px` → `440px`
- 字体提升：标签 `11px→12px`，提示 `9px→11px`，卖点 `11px/9px→12px/11px`，验证码按钮 `10px→11px`
- 前端 build 验证通过

## 4. 项目分析报告更新
- CasaMind 项目分析报告已同步更新：短信服务修复详情、技术栈脱敏、排版美化、真实验收结果

## 5. 认证补全三件套（找回密码 / 邀请链接 / 设备会话）
- **找回密码**：后端 `POST /auth/password/reset`（验码→重置→revoke 全部会话，AuthService.reset_password）；前端 AuthView 加 `reset` 模式（忘记密码入口、验证码 scene=reset_password、新密码强度条、成功回登录页提示）
- **邀请链接闭环**：后端 `GET /invitations/{token}` 公开预览（手机号脱敏 138****0077，expired 标记）；前端新增 `/invite/:token` 独立落地页 InviteView.vue（未登录引导登录后回跳，已登录可接受/谢绝）；MembersView 邀请记录加"复制链接"按钮（clipboard + prompt 降级）；router 守卫放行 invite 公开页
- **设备会话**：后端 `GET /auth/sessions`（JOIN 带家庭名、过滤过期）+ `DELETE /auth/sessions/{id}`（校验归属）；MembersView 新增"登录设备"面板（单个退出 + 退出所有设备）
- **测试**：新增 3 个测试（独立用户避免污染 session 级共享夹具！reset 会 bump token_version），pytest 39→42 passed，ruff 通过，vite build 通过
- 注意：conftest 的 auth_session/auth_headers 是 session 级共享夹具，写测试时不要对共享用户做改密/踢下线类操作

## 7. SSE 断线重连（2.6 节未完成项）
- **事件 ID**：`_sse()` 改为 `f"id: {event_id}\nevent: {event}\ndata: ..."`；`ConversationService._emit` 封装「next_event_id → append_event → yield _sse」三步
- **Redis 持久化**：`RuntimeStateService` 新增 6 个方法（next_event_id/append_event/get_events_since/set_turn_status/get_turn_status/clear_turn），全部带 Redis 不可用时的进程内降级
- **重放端点**：`GET /chat/sessions/{id}/events?after=xxx` 返回 `{events, turn_status}`
- **前端重连**：`streamChat` 追踪 `lastEventId`，流中断时调 `replayChatEvents` 轮询最多 3 次（2s 间隔），遇终端事件正常返回
- **验证**：42 passed，ruff 通过，vite build 通过

## 6. 项目约定备忘
- 后端 venv 无 pip，用 `uv sync`；uv 在本机有 safe-delete bug（Windows Recycle Bin 不可用），构建 sdist 依赖时需 `ensurepip` + pip 直装
- uv.lock 从未包含 alibabacloud 依赖（历史上就是 pip 直装），pyproject 与 lock 不完全同步是既有状态

## 7. BGE-M3 语义向量模型接入（报告 2.1 未完成项补齐）
- 新建 `app/services/embeddings.py`：嵌入后端工厂，`sentence-transformers` 可选导入，`local_files_only=True` 绝不隐式下载；依赖/模型缺失自动回退 Chroma 内置 ONNX MiniLM 并记 loguru 警告
- 配置：`EMBEDDING_PROVIDER`（auto/bge-m3/default）、`EMBEDDING_MODEL`、`EMBEDDING_MODEL_PATH`、`EMBEDDING_DEVICE`；pyproject 新增 `bge` extra（sentence-transformers）
- 维度隔离：BGE-M3（1024 维）激活时集合名自动变为 `casamind_knowledge-bge-m3`，与内置 384 维集合隔离
- 透出：`RetrievalDiagnostics.embedding` + `AIServiceStatus.embedding`（脱敏文案"本地语义模型 BGE-M3"/"内置轻量语义模型"）；前端 KnowledgeView 基础设施条新增"语义模型"指示，types.ts 同步
- 验证：pytest 47 passed（新增 5 项嵌入测试，用 stub module 模拟 sentence_transformers）；vite build 通过；5173 前端 200
- 待用户操作：模型需自行下载（约 2.2GB，hf-mirror/ModelScope/git-lfs 三选一，步骤已写入报告 2.1 节），下好后 `.env` 设 `EMBEDDING_PROVIDER=bge-m3` + `EMBEDDING_MODEL_PATH`

## 8. BGE-M3 真机部署与验证（用户已下载模型后续步骤）
- 用户已通过 ModelScope 下载模型至 `D:\software\tools\modes\bge-m3`（文件完整：pytorch_model.bin / tokenizer.json / sentencepiece.bpe.model / config_sentence_transformers.json 等）
- 依赖安装：venv 无 pip，用 `uv pip install --python .venv/Scripts/python.exe`；先装 `torch --index-url https://download.pytorch.org/whl/cpu`（CPU 版 116MB，得 `torch 2.13.0+cpu`），再装 `sentence-transformers`（得 `5.6.1`，连带 transformers 5.14.1 / scipy / scikit-learn；tokenizers 由 0.23.1→0.22.2）。UV_LINK_MODE=copy 抑制跨盘 hardlink 警告
- `.env` 追加：`EMBEDDING_PROVIDER=bge-m3` / `EMBEDDING_MODEL_PATH=D:\software\tools\modes\bge-m3` / `EMBEDDING_DEVICE=cpu`
- 真机验证：`create_embedding_backend` 解析为 `本地语义模型 BGE-M3`（is_bge_m3=True），`encode` 产出 1024 维；真 Chroma（localhost:8001）创建 `casamind_knowledge-bge-m3` 集合（count=0 待灌入），心跳正常；pytest 47 passed
- 小修：`knowledge.py` status() 的 `collection` 字段由 `settings.chroma_collection`（基础名）改为 `vector_store.collection_name_for(embedding)`（真实集合名，BGE-M3 时带 -bge-m3 后缀），前端显示与实际一致
- 后端以 `--reload` 模式运行于 127.0.0.1:8000；/ai/status 需鉴权（401 无 Token），SMS 走真实阿里云 Dypnsapi 不便造 token，服务层+真 Chroma e2e 已充分证明链路通畅
- 待用户 UI 确认：登录后知识库页"语义模型"应显示"本地语义模型 BGE-M3"；点"初始化知识"触发 POST /knowledge/bootstrap 将真实家庭文档以 1024 维灌入新集合

## 9. 用户截图反馈的 4 个体验问题根因修复
- **登录方式（密码 + 短信）**：两条端点都活着（`POST /auth/login` + `POST /auth/sms/login`），前端 `AuthView` 已有分段 tab 暴露，无需代码改动
- **仪表盘内容真实化**：`router.py:149` dashboard endpoint 原本用 `get_dashboard()` demo 数据填全部字段（greeting/date_label/notices/tonight_meal/budget/week_progress），与登录用户无关。修复：
  - greeting 按小时动态 + `context.display_name`（告别"小王"硬编码）
  - date_label 按 `date.today()` 动态
  - notices 从真实事件/预算/任务推导
  - 无活跃周计划时给占位（"尚未规划本周菜单"、清零预算、progress=0）而非假数字
  - Redis 缓存键 `dashboard:{family_id}` → `dashboard:{family_id}:{user_id}` 避免同家庭多账号互串
  - `_invalidate_family_cache` 改用新增 `RuntimeStateService.delete_prefix`（Redis SCAN+DEL）批量失效
  - pytest 47 passed（`test_dashboard_uses_authenticated_family` 仍 ok）
  - 真机验证：uid=5(小张) 返回 `family_name='小张的家'`, `greeting='下午好，小张'`, `date_label='8 月 5 日 · 周三'`, `notices=['本周预算已使用 94%，注意控制采购']`, `tasks=4`
- **路由切换空白页**：`router.ts` 新增 `router.onError` 监听 `Failed to fetch dynamically imported module|Importing a module script failed|Loading chunk ... failed`，触发 `window.location.assign(to.fullPath)` 自动 reload；用 `chunkReloadKey` 去重避免无限刷新。dev 下 Vite/HMR 重启后浏览器持有旧 chunk URL 是根因，生产构建无此问题但增强稳健性
- **"初始化知识"后语义模型未刷新**：根因不是前端，而是 `--reload` 模式下 worker 进程的 `vector_store._embedding` lru_cache 不自动失效，旧的 default backend 被持续复用。**不再用 --reload**，每次手动重启 uvicorn。干净启动后 `/ai/status` 立即返回 `"embedding":"本地语义模型 BGE-M3"` + `"collection":"casamind_knowledge-bge-m3"`；用 `POST /knowledge/bootstrap` 把 3 份引导文档灌入新集合

## 10. 调试技巧备忘
- 后端需要 token 调试时：用 Postgres users/family_memberships 查 uid/fid/role/ver，用 `jwt.encode(payload, settings.jwt_secret_key, algorithm=settings.jwt_algorithm)` 自签 JWT；payload 必含 `iss/sub/fid/role/ver/type/iat/exp/jti`；`ver` 来自 `users.token_version`
- SMS 走真实阿里云 Dypnsapi 不便为测试造 token；测试 conftest 用 `verify_code` mock 接 `123456`，live 后端无后门
- uvicorn `--reload` 在有 lru_cache 的进程里**会让缓存永不过期**，重启大改（Settings、向量后端等）必须用干净 uvicorn
- 仪表盘 demo 数据根除：`backend/app/services/demo_data.py:get_dashboard()` 是固定 mock，路由 endpoint 必须按 `context.family_id/user_id` 重算或从真实数据推导
- 路由懒加载 + Vite dev：dev 重启/HMR 后旧 chunk URL 失效，`import()` 失败导致空白页。`router.onError` 自动 reload 是稳健修复

## 11. 路由切换空白页尝试与回退（已撤销静态 import 方案）
- 曾尝试把 `router.ts` 所有 `() => import('./views/XxxView.vue')` 改为顶部静态 import，让路由切换不依赖动态 chunk
- 用户要求撤销，已回退到原始懒加载版本（保留 `router.onError` 兜底）
- 路由切换空白页问题若复现，回归"硬刷浏览器拿新 bundle"或重新审视具体 view 的 onMounted 加载逻辑
