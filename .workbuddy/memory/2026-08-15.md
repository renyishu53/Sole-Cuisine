# 2026-08-15

## 前端规划讨论（基于 6 阶段业务流程定稿）
- 用户确认首页定位：开门即见"今天吃什么 + 周进度"，引导进主流程（与之前 §2.1 一致，定稿化）。
- 用户提出增强：学习回流可视化不局限于周报，应支持日报（选具体某天）/ 周报 范围自选。
- 评估：均可行。后端 feedback 已有 `since/until` 时间过滤、周报按周一起始切窗，扩展日/周自选低成本。
- 边界注意：日报须与实时复盘页（`FeedbackView` 最近 30 条口味画像）区分定位——复盘页=系统学到什么（实时累积）、日报=某天执行回顾（吃了啥/打卡/花费/反馈）、周报=周维度归因。
- 建议：把周报页升级为"报告中心"，顶部范围切换（日/周），日报默认今天、周报默认本周。
- 首页与今日 Tab 边界必须守住：首页=轻量快照（3 数字 + 1 CTA），今日 Tab=操作台（打卡 + 营养进度 + 展开），否则回退到 B2（今日概览≈首页）老问题。
- 12:48 用户要求将两项变更（首页定位确认 + 日报/周报范围自选）重新写入「前端组件与按钮重规划」并纯文字输出（不落文件）。已整合输出合并版规范：首页 `HomeView` 轻量快照、报告中心 `ReportCenter` 取代独立周报页、三类看反馈视图（实时复盘/日报/周报）层次厘清。
- 12:58 用户追问"首页进度展示是否可含每日进度" → 可行且合理。决策：首页进度区从"仅周进度"扩展为"今日进度 + 周进度"双轻量指标（只读汇总数字/迷你条，如今日营养达标率或今日打卡 X/Y 餐），详细打卡与分项营养仍在今日 Tab，不重复。首页三件套 = 今天吃什么 + 今日进度 + 周进度。
- 13:17-13:25 重大布局纠正：**侧栏 = 分组导航（分组标题 + 子按钮），右区 = 内容面板（master-detail）**。用户两次打回：①不要侧栏内手风琴嵌套子按钮；②不要"点个人档案后右区再嵌套两个内容块"。正确形态参考用户截图：侧栏里「个人档案」是**不可点击的分组标题**，下方挂两个可点击子按钮「档案采集」「营养目标」；点子按钮→右区渲染对应内容。双 AI 入口（右栏修改对话写 / FAB 问 SoloChef 读）保持、位置不变。整体壳 = 左分组侧栏 + 右内容面板。
- 13:52 用户质疑"菜谱知识库 RAG 软知识数据太少"。核代码确认属实且薄在两层：①默认入库仅 `BOOTSTRAP_DOCUMENTS`=**3 篇**（knowledge.py L34-64：晚餐指南/控糖原则/备餐复用），`bootstrap()` 只灌这 3 篇；②`generate_recipe_docs.py` 生成的 30 篇独居快手晚餐 .md 虽已落盘 `app/data/knowledge_docs/独居快手晚餐/`（30 个文件确认存在），但需手动跑 `import_knowledge_docs.py` 才进 Milvus，无自启动钩子 → 全新部署向量库常态仅 3 篇。RAG 已接入规划链路（workflow.py L240 retrieve_graph / L266 retrieve_vector，top_k=4）。结构化对照 `recipes.json` 仅 **8 条且全 healthy**（无增肌/减脂分化）。根因：语料全为同主题"快手晚餐炒菜"，无早/午/汤/凉/主食/西式、无按目标(增肌/减脂)与忌口维度标签，无法按用户画像定向召回。建议：bootstrap 自动导入 knowledge_docs、按目标/餐次/约束扩类并加元数据标签、用已有 rag_eval 量化 Recall@k 前后对比。
