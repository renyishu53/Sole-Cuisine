# 2026-08-10 工作日志

## 简历项目经历精简调整

- 按用户要求调整 `SoloChef_简历项目经历.md`：
  - 技术栈改为按层顺序简要概括：前端 → 后端 → AI → 数据库/中间件 → 工程化
  - 技术亮点从 7 条精简为 4 条，聚焦 Agent 方向：① LangGraph 多智能体编排 ② 约束式生成三段式架构 ③ Graph RAG 双引擎混合检索 ④ 执行反馈闭环与口味画像学习
  - 移除营养闭环/工程化/架构收敛三条非 Agent 方向亮点与底部速查表

## 简历项目经历格式二次调整（按参考截图格式）

- 用户提供新截图，技术栈采用"分组（技术 + 技术 + ...）、分组（...）"格式，分组间用顿号分隔
- 技术亮点采用 3 条加粗标题 + 冒号 + 一段完整描述的项目符号列表，标题与描述之间无空行，更贴近截图风格
- 精简为 3 条核心 Agent 方向亮点：① LangGraph 多智能体编排 ② Graph RAG 双引擎混合检索 ③ 执行反馈闭环与口味画像学习
- 将"约束式生成三段式架构"的能力融入第 1 条 LangGraph 亮点中（Pydantic 强校验结构化中间结果 + 8 道 Verifier 校验）

## 简历项目经历格式三次调整（按参考截图格式）

- 用户第三次发同一张截图，强调"技术栈简要概括、按顺序（前端→后端→数据库等）"
- 精简技术栈：去掉版本号（Python 3.12→Python、SQLAlchemy 2.x→SQLAlchemy、MySQL 8→MySQL 等）、合并重复项、删除 Alembic（与 SQLAlchemy 重复）、删除 LangChain（被 LangGraph 覆盖）
- 最终：前端 4 项 / 后端 4 项 / AI 4 项 / 数据库·中间件 4 项 / 工程化 5 项 = 21 项，整体接近截图简洁度

## 简历项目经历格式四次调整（极致精简）

- 技术栈每组精简至 3 项：前端 Vue 3 + TypeScript + Pinia；后端 FastAPI + Python + SQLAlchemy；AI LangGraph + DeepSeek + BGE-M3；数据库 MySQL + Neo4j + Chroma；工程化 Docker Compose + pytest + GitHub Actions
- 技术亮点描述去除所有括号与斜杠：用顿号替代括号内枚举、用"与"替代斜杠分隔、Recall@k/nDCG@k 改为 Recall 与 nDCG
- 最终 3 条亮点均为纯文字描述无符号干扰

## 新增「专业技能」板块

- 用户提供 4 张截图：图1/2 为项目经历参考（SoloChef 已完成 + 山野智行 + 新能源汽车推荐系统），图3 为待修改的「专业技能」（当前纯前端方向），图4 为参考版「专业技能」（AI Agent / 后端方向）
- 在 `SoloChef_简历项目经历.md` 末尾新增「专业技能」板块，共 8 条：
  1. AI Agent 与多智能体编排（LangGraph / LangChain / DeepSeek）
  2. Graph RAG 混合检索架构（Neo4j / Chroma / BGE-M3 / bge-reranker）
  3. Python 后端开发（FastAPI / SQLAlchemy async / Pydantic / Celery / Redis）
  4. 数据库设计与优化（MySQL / Alembic / Neo4j Cypher / Chroma HNSW）
  5. 前端工程化（Vue 3 / TypeScript / Pinia / Axios / SSE / ECharts / Element Plus）
  6. 容器化部署与 CI/CD（Docker Compose / GitHub Actions 四道门禁）
  7. 代码质量保障（pytest / ruff / mypy 三重门禁 / 容错降级设计）
  8. 工程工具与协作（Git Conventional Commits / uv 包管理）
- 格式对齐图4：每条 • 开头 + 加粗关键词 + 冒号 + 描述，无括号无斜杠

## 专业技能重写（基于三个项目综合能力）

- 用户提供两张项目经历截图，包含三个项目：
  1. SoloChef — AI 独居膳食与采买规划师（LangGraph / Neo4j / Chroma / BGE-M3）
  2. 山野智行 · 户外出行平台（Redis 购物车 / 支付宝 RSA2 / 三存储分离 MySQL+Redis+ES / LangGraph RAG 对话 SSE）
  3. 新能源汽车智能推荐系统（Dify 双工作流 / JWT+RBAC 鉴权 / 文件上传三重防御 / 订单七状态状态机）
- 重写「专业技能」板块为 8 条，覆盖三个项目的综合技术能力：
  1. AI Agent 与多智能体编排（LangGraph StateGraph + Dify 双工作流 + SSE 三段式链路）
  2. Graph RAG 混合检索架构（Neo4j + Chroma + BGE-M3 + bge-reranker）
  3. Python 后端开发与鉴权体系（FastAPI + SQLAlchemy + JWT + RBAC + bcrypt + 文件上传防御）
  4. 多存储架构设计（MySQL 事务 + Redis 缓存/购物车 + ES 全文检索 + 搜索降级）
  5. 复杂业务架构设计（交易闭环 / 支付宝 RSA2 / asyncio 超时回补库存 / 七状态机 / 补偿重放）
  6. 前端工程化与数据可视化（Vue 3 + TypeScript + Pinia + Element Plus + ECharts + SSE）
  7. 容器化部署与持续集成（Docker Compose + Nginx 假流式修复 + GitHub Actions 四道门禁）
  8. 代码质量保障与工程工具（pytest + ruff + mypy + Git Conventional Commits + uv）

## 专业技能按图片格式重写（适配 AI 应用开发实习生岗位）

- 用户提供参考截图（8 条技能，"熟练掌握/熟悉/了解" + 加粗关键词 + 简洁描述风格）
- 目标岗位：AI 应用开发实习生
- 重写为 8 条，格式对齐参考图：
  1. **熟练掌握** AI Agent 框架（LangGraph / LangChain / Dify）— 多智能体协作系统与完整工作流
  2. **熟练运用** RAG 架构与向量数据库 — Neo4j+Chroma 双引擎混合检索 + BGE-M3 嵌入 + bge-reranker 精排
  3. **熟练使用** Django 与 FastAPI — RESTful API 设计与全栈集成 + SQLAlchemy ORM + Pydantic 校验
  4. **熟悉** MySQL 设计优化 — 索引/事务/调优；了解 Neo4j/Chroma/Redis/ES 多存储架构
  5. **熟悉** Coze 与 Dify 低代码 AI 平台 — 拖拽式工作流 + DeepSeek 集成
  6. **熟练掌握** HTML/CSS/JS/TS 及 Vue 3 — Element Plus/ECharts 前端界面 + Axios/SSE 交互
  7. **熟悉** Docker 容器化部署 — Dockerfile 多阶段构建 + Docker Compose 多服务编排
  8. **熟练使用** Git 版本控制 — 分支管理 + Conventional Commits；了解 pytest/ruff/mypy 质量工具

## 去家庭残留清理（P3 收尾，commit 5d96133）

- 起因：用户核实发现 evaluation.py 仍是家庭规划残留（MemberProfile + members 恒为空 → 忌口校验永远 100% 失效）；三个 agent 签名挂 MemberProfile/CalendarEvent 残留；workflow.py 有 _empty_calendar_result 垫片
- 8 文件清理：
  - evaluation.py：evaluate_plan 去 members，改读单人 UserProfile.constraints，忌口校验真正生效
  - domain_agents.py：meal/shopping/budget 的 members/events 收口为可选默认空；meal 新增 constraints/preferences 参数优先取 UserProfile，members 遗留回退
  - workflow.py：WorkflowState 加 user_constraints/user_preferences；run() 注入；_meal_agent_node 传约束；_verifier_node 加单人约束路径；删 _empty_calendar_result 与 PlanningResponse.calendar 构造
  - planning.py：新增 _load_user_profile_constraints 加载 UserProfile 注入 workflow
  - schemas/domain.py：PlanningResponse 删 calendar 字段（前端不消费）
  - 删孤儿 calendar_planning.py（analyze_calendar 无调用者）
  - test_rag.py：去掉过时 response.calendar 断言
- 验证：pytest 54 passed / ruff 全绿 / mypy 53 源文件无问题（54→53 因删 calendar_planning.py）
- 保留项：draft.tasks 保留（前端 PlanDetailView 在用，是 SoloChef 执行辅助项非死代码）
- 未动：members/CalendarEvent 仍在 knowledge/graph_store/conversation 等服务签名中作为可选参数（恒空），属更广残留面，本次未扩张

## RAG 真实端到端验证 + embeddings 修复（commit 96a772e）

- 用户要求起 docker-compose 跑真实检索冒烟，验证 RAG 流程端到端。
- 底座：docker compose 起 redis/neo4j/chroma（mysql:8.0 镜像未缓存、无网拉取，但 RAG 检索不依赖 SQL，故只起三者）。neo4j 持久化卷当初用旧密码初始化，重置卷（project_agent_neo4j_data）使其按 .env 的 solochef_password 重建。
- 新增 `backend/scripts/rag_smoke_test.py`：连真实 Chroma+Neo4j，播种 3 篇 BOOTSTRAP_DOCUMENTS（ingest_text 同时写向量+图谱实体），调用 knowledge.retrieve() 断言 chroma_status=connected 且 vector_hits>0、neo4j_status=connected。含旧集合清理步骤保证用修复后的 embedding 函数重建。
- **冒烟暴露出被单元测试 stub 漏掉的真实 bug**：`SentenceTransformerEmbedding` 与 Chroma 1.5.9 接口不兼容：
  1. 缺 `embed_query`/`embed_documents` → query 路径 AttributeError
  2. `embed_query` 返回值类型错（先嵌套 list 触发 ValueError，后 Python list 触发 chroma convert_np_embeddings_to_list 的 AttributeError）→ 正确做法是返回 model.encode 原始 numpy 输出
  3. 补 `is_legacy=False` 消除弃用警告
- 修复 `app/services/embeddings.py` + 同步 `test_rag.py` 断言（embed 函数返回原始模型输出）。
- **验证结果 PASS**：chroma_status=connected + vector_hits=3（语义相关，如"控糖饮食"→控糖饮食原则.md score=0.76）+ neo4j_status=connected。
- 注意：reranker（bge-reranker-v2-m3）本地无权重、无网下载 → ConnectTimeout，优雅降级 rerank_status=disabled（可选增强，非核心检索）。
- 验证：pytest 54 passed / ruff 全绿 / mypy 53 源文件无问题。

## 回滚 /python-patterns 改动并按 ①②③ 顺序重做（收口，未提交）

- 动因：用户要求把代码恢复到触发 `/python-patterns` 之前的状态，再从干净基线按 ①前端↔后端 e2e 联调 → ②G07 餐食替换营养联动 → ③G14 门控真实 MySQL 集成测试 → 顺手修报告漂移 + G17 去家庭化残留 的顺序重做，复用 python-patterns 规范与项目既有风格。
- 回滚：`git reset --hard 55aed74`（pre-/python-patterns 基线）+ 备份 tag `backup/pre-python-patterns-20260810`（→9069a65）。
- 重应用：4 次独立 `git cherry-pick --no-commit` 把 G14/G07/报告/G17 后端提交重新落地（多提交 cherry-pick 不支持 --no-commit）。
- ① 联调真实断点修复：清幽灵 `/inventory` 调用（api.ts 删 3 方法）+ ShoppingView 库存面板移除；NutritionGoalView 接真实 `/profile`+`/profile/nutrition-goal`（404 静默）；**MealsView.vue 空状态 v-else 误挂缺陷**——`<p v-if="replaceResult?.feedback">` 与 `<section v-else>` 配对导致有餐食也显空状态，拆为独立联动面板 + `v-if="!data.length"`。
- ② G07：`replace_meal` 返回 `MealReplacementOutcome`（单餐/全天营养重算 + 购物清单按 `餐食:{id}` source 增删合并去重）；前端 types.ts 扩展 + MealsView 联动面板。
- ③ G14：门控 `test_mysql_integration.py`（需 `SOLOCHEF_TEST_MYSQL_URL`）+ DDL 审计。
- G17：跨 10 文件移除 `members`/`events`/`CalendarEvent` 残留参数。
- 报告 v9.1：同步前端 vue-tsc 通过、模块 8→9、缺口 2→1（仅 G11）；批量修正行号漂移（router.py L858-930→L397-482 等 7 处）。
- **终检全绿**：ruff All checks passed! / mypy 53 源文件 Success / pytest 55 passed + 1 skipped（gated MySQL）/ 前端 vue-tsc -b --force exit 0。
- 状态：全部改动未提交。已暂存 17（报告+后端10++3 测试），未暂存 7（报告行号补丁 + nutrition.py PEP8 空行补丁 + 前端 5 文件）。等用户 review 后提交。
