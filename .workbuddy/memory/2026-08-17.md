# 2026-08-17 工作记录

## 任务：生成 SoloChef 项目技术拆解文档
- 产出：`SoloChef项目技术拆解文档.md`（项目根目录），模仿"山野智行"讲解话术模板，**每站先业务流后技术实现**，含开场白/12站/收尾/应急问答/裁剪指南。
- 用户需求：认真读全部代码 + 参考模板 + 生成技术拆解文档。用户此前偏好"纯文字不落文件"，但本次明确要求"生成文档"，故落文件。
- 注意：用户中途发了"恢复刚刚的变更""请继续完成未完成的任务"——理解为继续完成（被外部AI中断的）文档生成任务，已恢复并完成。

## 代码核验关键结论（写文档时确认）
- **多智能体**：`ai/workflow.py` 11 节点 StateGraph（intent→graph_retriever+vector_retriever→coordinator→meal/shopping/budget agent→domain_coordinator→planner→verifier→final_planner）；`ai/domain_agents.py` 的 `StructuredDomainAgentEngine` 三个领域智能体，各有 LLM 路径 + 确定性兜底，`reconcile_budget()` 内部消解预算硬冲突。
- **RAG**：`services/knowledge.py` 双底座 Milvus(向量)+Neo4j(图谱)，`retrieve()` 并发召回 + 可选 reranker 降级；bootstrap 幂等；`entity_extraction_llm_enabled` 默认 False（本地正则抽取）。
- **反馈闭环**：`services/feedback_loop.py` `capture()` = 落库 `plan_feedback` + 回图谱 + 回向量滚动文档 + 忌口自动纳入（`apply_constraint_rules` 连续3次负反馈写 constraints）+ 口味画像 `taste_profile` 注入餐食 agent（确定性路径也用）。
- **周计划代码修正**：`planning.py` 已有 `current_week_start_utc()` / `is_current_weekly_plan()`（校验 created_at 在本周且覆盖 **21 餐**槽位 == `_EXPECTED_MEAL_SLOTS`），但 `get_active_plan()` 仍只查 `is_active=True` 无周过滤。业务定义 = 21餐/周（7×3）。之前对话说"完全缺周归属"需修正。
- **营养目标**：`services/nutrition.py` Mifflin-St Jeor TDEE × PAL × 目标系数；宏量按 DRIs2023/ISSN；等价物解释（鸡胸肉/米饭）。
- **鉴权**：`auth_router.py` JWT双令牌(30min/7d) + 阿里云短信(Redis TTL) + bcrypt + token_version 吊销 + 设备会话；前端 `api.ts` axios 拦截器 401 单飞刷新。
- **端点规模**：router.py 约 80+ 端点（含 /plans, /meals, /shopping, /feedback, /chat, /knowledge, /recipes, /admin 等）。
- **部署**：docker-compose 9 容器（backend/worker/frontend/mysql/redis/neo4j/etcd/minio/milvus），MySQL healthcheck 控制启动顺序。

## 待办/已知短板（已写入文档收尾）
- RAG 默认语料薄（BOOTSTRAP 3篇 + knowledge_docs 目录默认不自动全量入库）
- 首页只在 onMounted 拉一次不自动刷新
- 周轮转自动化（get_active_plan 未接周过滤）
- 文档滞后：API接口文档.md 仍残留 chroma_* 字段名（已确认 pyproject.toml:83 `chromadb.*` 残留 + 5文档旧描述）
- 口味演化/画像展示区块：用户确认永久下线，外部AI工具正在执行路径A清理（删展示出口保留底层学习），截至本文档生成时 router.py 仍有 /meals/taste-profile 与 /feedback/taste-vector 端点（清理可能尚未落地或文件系统为清理前状态）。

## 任务：Agent 技术深度拆解（用户要求"只文字不落文件"）
- 用户追问 agent 部分技术深度，已读 workflow.py / domain_agents.py / prompts.py / llm.py / vision.py / segmented_planner.py / plan_revise.py 全文，纯文字输出拆解（未改文件）。
- 关键深化点（对上一文档的补充）：
  - **提示词版本注册表** `ai/prompts.py`：meal/shopping/budget 各自多版本（meal 1.0→1.3 接入忌口→口味画像→生活约束；budget 1.2 引入"分类之和+预留==周预算"硬性等式 + self_check 字段）；`get_active()` 取最新，`list_versions()` 审计。
  - **模式串编码版本**：domain agent 返回模式串 `deterministic:vX` / `llm:vX` / `deterministic-fallback:vX`，用于评测溯源。
  - **reconcile_budget 双层防御**：提示词等式约束 + 代码按比例 scale 压缩兜底 → 前端永不收"分类超预算"硬冲突（=用户此前确认的"预算硬冲突系统内部消解"代码实现）。
  - **Plan Generator 三实现**：DemoPlanGenerator（确定性21餐）/ OpenAICompatiblePlanGenerator（真LLM流式+token_sink）/ SegmentedPlanGenerator（P3 分段 meals→shopping→budget，默认关闭，失败回退单次）。工厂 `build_plan_generator()` 按 settings 选。
  - **ChatAssistant**（对话，temperature0.6流式，不强制JSON）+ **VisionService/Qwen-VL**（独立vlm配置，5场景SCENE_PROMPTS，图片预处理2048px/JPEG85/base64，结构化JSON校验）—— 两者均与文本LLM链路解耦、懒加载单例、未配置返回None。
  - **plan_revise LLM 解析双通道**：demo 规则兜底 `_parse_demo_operation` + 真LLM `bind(json_object)` Pydantic校验失败重试2次；预览/确认分离（revise存payload，confirm落库派生新版本）。
  - **verifier 三级策略**：L1 apply_auto_fix（重复→缺天→营养→预算，≤2轮防震荡）/ L2 软冲突消解 / L3 硬冲突率>30%→人工接管。
  - **checkpointer 当前为内存版**（InMemorySaver），进程重启丢状态；需换持久化版才支持跨请求 resume。

## 重要更正：项目并非"默认 demo 模式"（用户问"没配llm_api_key吗"）
- **`.env` 已配置真实密钥**：`LLM_PROVIDER=deepseek` + `LLM_API_KEY=sk-...`（已设）+ `LLM_MODEL=deepseek-chat`（config.py 默认值才 demo/空，被 .env 覆盖）。运行时 `real_llm_enabled=True` → **主规划器走真实 DeepSeek LLM，不是 demo 模板**。
- **分档（运行时真实状态）**：
  - 主规划器 planner：✅ 真 LLM（DeepSeek）
  - 3 领域智能体 meal/shopping/budget：⚠️ 仍确定性 fallback（`.env` 无 `DOMAIN_AGENTS_LLM:False`→`config.py:59 domain_agents_llm_enabled=False` 默认关；加 `DOMAIN_AGENTS_LLM_ENABLED=true` 即开真 LLM）
  - 视觉 Qwen-VL：✅ 真 VLM（`.env` `VLM_ENABLED=true`+key）
  - 实体抽取/分段生成：❌ 关（对应开关未覆盖，默认 False）
- **安全**：`.env` 含明文真实密钥（DeepSeek/VLM/阿里云短信/JWT），已确认 `.gitignore` 第2行忽略且 `git ls-files` 未跟踪（无 git 泄露风险）；勿外传，曾外传则轮换 key。
- ⚠️ 之前多轮对话(含"当前项目只是一个demo吗""首页/demo模式"等)把项目说成"默认 demo 模式/未配 key"是基于 config.py 默认值、未看 .env 的**误判**，本次已更正。

## 任务：完整技术拆解文档（用户要求"梳理业务流程+详细技术拆解，重点拆解agent"）
- 产出：`SoloChef_技术拆解文档.md`（项目根目录）。通读 backend 全部核心代码后撰写，含七章：概览/架构总览/业务流程/【Agent技术拆解(重点)】/设计模式/已知限制/总结。
- Agent 拆解覆盖：LangGraph 11节点 StateGraph(int workflow.py)、StructuredDomainAgentEngine 三领域智能体(domain_agents.py)、提示词版本注册表(prompts.py)、Verifier 三级自愈(plan_validation.py)、反馈闭环(feedback_loop.py)、备餐修改(plan_revise.py)、对话助手(ChatAssistant)、视觉(Qwen-VL)、评测(evaluation.py)、分段生成(segmented_planner.py)、检查点(InMemorySaver)、降级矩阵。
- 数据层确认 14 张表(`app/models/identity.py`)，核对 endpoint 87+（router.py）。
- 与上一文档差异：本篇是"全栈技术拆解+业务流程+agent深拆"整合版，非纯文字、已落文件。

## 任务：四大 AI 技术点核查（工具调用/断点续传/短期记忆/长期记忆）
- **工具调用 Tool Calling：❌ 未实现**。全 app 搜 `bind_tools/@tool/tools=/tool_calls/function_call` 零命中；所有 LLM 调用均 `.bind(response_format={"type":"json_object"})` 结构化输出，`domain_agents.py:131 available_tools` 仅为厨具清单变量。RAG 检索等"动作"硬编码进 LangGraph 节点，未暴露给 LLM 自主调用（有意取舍：确定性兜底优先）。
- **断点续传：✅ 实现但仅进程内**。`checkpoints.py` 用 `InMemorySaver`（重启即丢，注释已写明可换 `langgraph-checkpoint-redis`）；`workflow.py` `set_checkpointer`+`run(resume=True)`+`aget_state(thread_id)`；`planning.py` `resume()` 已接，`router.py:1965` 暴露恢复端点。局限：非跨进程持久化。
- **短期记忆：✅ 两类手写**。(a) `WorkflowState` TypedDict = 单次 run 草稿纸，run 后丢弃；(b) 对话多轮：`conversation.py:162 _extract_history` 取最近 8 条 `ChatMessage`，`llm.py:254` 注入 `history[-8:]`；无 LangChain Memory 抽象、无超长会话摘要。
- **长期记忆：✅ 已建闭环（最扎实）**。`feedback_loop.py` `capture()` 落库 `plan_feedback`+回 Neo4j 图谱+回向量滚动文档(feedback-{user}-{type} 窗口30)；`replay()` 补偿未同步；`apply_constraint_rules` 连续3次负反馈自动写 `UserProfile.constraints`；`taste_vector_from_tags` 五维口味向量注入下一轮 meal agent。闭环：执行→反馈→写回图谱/向量→下轮规划检索命中→个性化收敛。记忆架构图已用 show_widget 输出。

## 任务：按"流程×技术栈"重新拆解（用户要求"各业务流程分别用到了哪些技术栈+如何实现"）
- 产出：`SoloChef_业务流程与技术拆解.md`（项目根目录）。结构：全局技术栈速查表 + 10 个业务流，每个流程拆「用到的技术栈 → 代码级如何实现(file:line)」。
- 覆盖流程：1 建档画像 / 2 营养目标(Mifflin-St Jeor纯Python) / 3 周计划生成(LangGraph 11节点+OpenAI兼容DeepSeek+Milvus+Neo4j+domain agents+verifier) / 4 局部修改(plan_revise 7类操作) / 5 执行反馈闭环 / 6 对话问答(SSE+8轮窗口) / 7 视觉(Qwen-VL 5场景) / 8 知识入库(Celery+Redis多队列) / 9 断点续传(InMemorySaver) / 10 前端(Vue3, timeline未落地)。
- **核实修正**：向量库实际仅 **Milvus**（`milvus_store.py`，`knowledge.py:76` 用 `MilvusVectorStore`），无 Chroma（早前 memory 笔记里的"Chroma"是旧实验遗留，已更正）；LLM 走 OpenAI 兼容协议 `ChatOpenAI(base_url=https://api.deepseek.com/v1, model=deepseek-chat)`（`llm.py:95`）；VLM 为 Qwen-VL(`qwen-vl-max`, DashScope `vision.py:140`)；Celery broker/backend=redis，`worker.py` 三队列 knowledge/graph/maintenance + DeadLetterTask 重试3次。

## 任务：用户梳理业务流，逐项核对+补充（19:06 轮）
- 用户自述主链路：注册登录→首页未建档引导→档案采集→营养目标→周计划生成(三智能体/LangGraph)→调整保留版本历史可回退→每日菜谱打卡反馈→购物清单CRUD→日报周报。
- **逐条核对结论（全部对得上代码）**：
  - 注册登录 ✅ `auth_router.py`/security.py(bcrypt+JWT双令牌)/AuthView.vue
  - 未建档引导 ✅ HomeView.vue 未建档banner+引导卡，profile_complete 闸门
  - 建档→营养目标引导 ✅ ProfileCollectionView「前往营养目标」按钮→/profile/goals；NutritionGoalView.vue
  - 周计划三智能体 ✅ workflow 11节点+LangGraph
  - 版本历史/回退 ✅ `derive_plan_with_modifications`+`list_plan_versions`+`POST /plans/{id}/rollback`；WeeklyPlan.version 列 + uq_weekly_plan_user_version 唯一约束（强证据）
  - 每日菜谱打卡反馈 ✅ `MealCheckinRequest`+`POST /meals/{id}/checkin`+`PlanFeedback`
  - 购物清单 CRUD ✅ /shopping CRUD+merge
  - **日报和周报：⚠️ 仅周报**。`WeeklyReportService`+`/reports/weekly`+WeeklyReportView.vue；**日报无独立报告**，仅有当日营养快照 `/meals/today/nutrition`（实时计算非生成报告）。
- **重要更正：intent 真路由已落地（服务层）**。之前说"`intent`节点是硬编码占位"——代码已演进：`app/ai/intent_router.py` 是**确定性词典分类器 `IntentRouter`**（无需LLM，CREATE/QUERY/REVISE/REGENERATE/SHOPPING，抽预算/蛋白/星期/餐次约束），**真正生效在 API 层** `router.py:1616 classify_assistant_intent` 决定子图；`workflow.py:245 _intent_node` 现调用 `intent_router.classify()`，仅把 `source_decision` 写进 trace 审计，子图仍固定 weekly_plan。即"真路由=服务层 IntentRouter，工作流节点=审计留痕"。（早前 memory 说 intent 纯占位需补此更正）
- **用户未覆盖的并行/支撑流（已补充给其）：视觉识别Qwen-VL、RAG问答AskSoloChef(SSE+8轮)、营养知识库管理(Celery同步)、食材替换/预算执行闭环、反馈闭环→学习(长期记忆反哺下轮)、多端设备会话、断点续传。**
- 已用 show_widget 输出「SoloChef 业务流全景」SVG（主链路+并行流+意图路由层+学习闭环）。
